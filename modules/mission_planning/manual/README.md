# Mission Planning Manual Tools

This folder holds operator-facing tools, manual validation utilities, and
reference/archive material that should not live loose in the
`modules/mission_planning` package root.

Compatibility wrappers may remain at the package root while existing launch
paths are still supported.
