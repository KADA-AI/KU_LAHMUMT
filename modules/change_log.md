2026-07-29 v1.5.27 - 공격 재계획의 계산 시간을 산출물·ICD·기능 변경 없이 줄였다. `Scenario_2026-07-29T124109`의 실측 프로파일에서 총 3,952~7,718ms 중 `apply_attack_plan_overrides`가 약 90%였고, 그 안에서 런타임 설정값 한 개를 읽는 데 약 1,600us가 들고 있었다. 원인은 조회마다 프로젝트 루트를 파일시스템으로 재탐색하고, 레거시 설정 파일 존재를 다시 확인하고, 설정 파일을 stat 한 뒤, 설정 문서 전체를 deepcopy해서 값 하나를 꺼내던 것이다. 설정은 웨이포인트마다 읽히므로 이 비용이 경로 생성 전반에 곱해지고 있었다. 프로젝트 루트와 레거시 마이그레이션 결과를 1회만 계산하도록 캐시하고, 단일 값 조회가 문서 전체를 복사하지 않도록 읽기 전용 섹션 뷰를 추가했으며, `capture_physics`·`capture_geometry`의 설정 조회도 같은 경로로 전환했다. 재계획 1회 동안 설정을 고정하는 `pin_runtime_settings()` 스코프를 도입해 조회당 stat도 제거했고, 이는 재계획 도중 설정이 바뀌어 하나의 계획이 두 설정으로 섞이는 것도 함께 막는다. 또한 스캔 좌표의 유무만 판정하면 되는 13개 지점이 최대 514개 좌표 리스트를 전부 검증·복사한 뒤 버리고 있어, 첫 유효 좌표에서 단락하는 술어로 교체했다(동치성 9케이스 검증). 측정 결과 설정 조회는 약 1,600us에서 6.4us(핀 적용 시 0.94us), 고도 rate-limit 패스는 3.4ms에서 0.2ms, 진입 안정화는 1.0ms에서 약 0.0ms, 실제 로그 경로 8개의 후처리 시퀀스는 243ms에서 61ms가 되었다. 설정 파일 외부 편집 감지와 값 정확성은 그대로 유지되며 modules 전체 1,112개 테스트를 통과했다(4개 skip).

2026-07-29 v1.5.26 - `Scenario_2026-07-29T112020`의 3갈래 AREA 수행 중 표적 15·14 발견 공격 재계획이 2.83초 동안 공격점 두 개를 실제로 생성하고도 `boundary guard ... inconsistent sequence count` 검증 오류로 폐기되어 UAV가 추적만 계속하던 문제를 수정했다. 진행 중인 Type2 AREA 경계 자식과 뒤이어 복제된 미수행 자식이 각각 원래 순번(3/5)과 부분 순번(1/2)을 유지하던 것을, 재계획 IMP의 실제 실행 순서에 맞춰 남은 경로 전체를 1..N으로 다시 번호 매기고 새 WP ID 기준 순환 링크로 재구성하도록 공통화했다. 촬영이 끝나 단일 완료 대기점으로 축약된 경로에서는 경계 순환 계약을 제거해 가짜 AREA 자식으로 검증되지 않게 했고, 공격 제외안의 전체 artifact ID 교체 뒤에도 경로 간 순환 링크를 다시 연결하도록 보완했다. 오류나 `missionUpdates`가 없는 불완전한 공격 제외안은 GUI의 선택 가능 계획으로 게시하지 않게 했다. 당시 보관된 0902 요청을 복제 DB에서 재생하여 공격 특화안에서 표적15→LAH2·표적14→LAH3의 공격 경로 두 개와 6대/36경로 전체 검증 통과, 공격 제외안의 6대/30경로 검증 통과를 확인했으며 modules 전체 1,095개 테스트를 통과했다(3개 skip).

2026-07-29 v1.5.25 - `Scenario_2026-07-29T032659`의 Type2 3갈래 LINE에서 공격 재계획 후 일부 분기가 거의 촬영되지 않은 채 완료되던 문제를 수정했다. UAV5 Path500000169는 58.7km의 남은 센서 sweep을 유지한 채 첫 촬영 carrier만 현재 위치 근처의 155.3m activation anchor로 이동하여 `searchSpeed`가 1,787.89m/s에서 19,648.29m/s로 폭증했고, SIM 센서가 86개 촬영점을 약 2.85초에 건너뛴 뒤 해당 분기를 완료로 판정했다. 공격 중인 UAV와 계속 촬영하는 UAV 모두 Type2 outbound/return carrier 좌표를 변경하지 않도록 통일하고, 공격·선행임무·공격 후 복귀의 남은 sweep 속도는 기존 직렬화 값을 매번 1.3배 증폭하지 않고 고정 carrier와 현재 위치의 기하에서 `search_speed_weight × resume margin`을 한 번에 재산정하도록 하여 반복 재계획을 멱등화했다. 선행임무 협업 재할당에서도 AREA 생성 속도는 그대로 보존하고 LINE만 실제 진입 leg 기준으로 동기화한다. 또한 공격 후 복귀 시 exact current plan의 Type2 진행 자료가 없을 때 더 큰 ID의 미선택 option에서 `complete` 상태를 가져와 현재 분기 suffix를 삭제할 수 있던 latest-plan fallback을 금지했다. 세 분기의 소유권과 후속 Input5~8 보존, 실측 19,648.29m/s 재현, 공격/선행/복귀 반복 속도 불변, AREA 속도 보존 회귀를 추가하여 modules 전체 1,062개 테스트를 통과했다(3개 skip).

2026-07-29 v1.5.24 - `Scenario_2026-07-29T032659`에서 target13 공격점이 정상적으로 `DEM 383m + 안전여유 30m = 413m`로 산출됐음에도 은폐 경로 탐색 실패 후 direct fallback이 당시 LAH3 실시간 고도 1841m를 공격점 최저고도로 덮어 WP29267에 계획 팝업을 만들던 문제를 수정했다. `attack_altitude_control=sim_los_popup` 공격점은 단일·연속 공격 package builder의 좌표 정규화 뒤에도 저고도/팝업 메타데이터를 보존하며, 현재 기체 고도 floor를 적용하지 않고 DEM 안전고도를 최종 공격 WP와 개별임무에 그대로 사용한다. 연쇄 원인이던 직전 공격 후 복귀도 보완했다. SIM 팝업·하강 중 재계획이 들어오면 post-attack connector가 일시적인 실시간 상승고도 대신 직전 armed 공격 WP의 저고도 base를 사용하고, SIM은 계획 교체 뒤에도 `descending_after_attack` 상태를 유지하며 새 경로의 동일 위치 고고도 시작점을 base 고도로 재기준화해 하강이 취소되거나 다시 상승하지 않게 했다. 실제 target11 로그의 2327m 실시간 팝업 위치를 계획 base 419m로 복원하는 것을 확인했고, direct fallback 413m 유지·원거리 오탐 방지·계획 교체 중 하강 완료 회귀를 추가하여 modules 전체 1,056개 테스트를 통과했다(3개 skip).

2026-07-29 v1.5.23 - `Scenario_2026-07-29T024517`에서 target11 격파 후 복귀 계획 700000018이 생성되지 않고 재계획 Queue가 120.244초 동안 멈춘 문제를 수정했다. 증분 공격으로 보존된 LAH 공격 임무의 `relatedMission.inputMissionID=3`과 현재 추적 UAV6의 `current_input_mission_id=4`가 다른 정상 상태를 복귀 대상 판정에서 잘못 배제하여 target9·target11의 종료된 공격 WP가 남았고, 연속성 검증 실패 결과에도 종료 0305가 발송되지 않은 것이 직접 원인이었다. 공격 후 복귀는 이제 inputMissionID가 아니라 전역 targetID로 모든 확인된 격파 표적의 LAH 임무·경로·공격 WP를 제거하고 살아 있는 후속 공격만 보존한다. 결과 계획이 없는 비정상 종료는 반드시 실패 0305를 보내 Queue를 즉시 해제하며, 불필요한 복귀만 정상 no-op로 끝낸다. 또한 오래된 attack_plan_id를 가진 추적 UAV도 현재 MissionPlan의 동일 UAV·target 임무가 실제로 남아 있을 때만 안전하게 복구하고, 0402 최신값 병합 중 새 격파 전이가 덮이지 않도록 terminal-aware FIFO를 추가했다. 실제 700000016 재생에서 UAV4-target9(artifact 복구)와 UAV6-target11(직접 계보)을 모두 확인했고, 모니터링 72개·공격/복귀 확대 53개 및 modules 전체 1,021개 테스트를 통과했다(3개 skip).

2026-07-28 v1.5.22 - 초기임무계획 및 동일 생성기를 사용하는 재계획의 산출물을 바꾸지 않고 실행시간을 단축했다. UAV 0303은 한 빌드 안에서 이미 고정한 runtime payload를 capture-physics 계산에 전달하고 lineSearch 좌표 상한도 같은 thread-local 설정 캐시에서 읽게 하여, 실제 `Scenario_2026-07-28T231231` 37경로·149WP의 timestamp 제외 JSON byte/hash를 그대로 유지하면서 cold 2.42초→0.99초, warm 1.80초→0.42초로 줄였다. LAH 0304는 terrain smoothing/저지대 탐색 설정을 빌드당 한 번만 읽어 전달하고 LINE/AREA 포함 판정을 Shapely prepared geometry로 재사용하여, 24경로 산출물과 모든 좌표·고도·속도·ETA·ECF·WP ID를 동일하게 유지하면서 단독 warm 약 1.06초→0.79~0.81초로 줄였다. 유·무인기 cold 동시 재생 4회도 기존 24경로·224WP와 동일 normalized hash를 모두 유지했고 FlightPath 생성 wall time은 기존 운영 로그 중앙값 약 3.43초에서 약 2.08초로 감소했다. 초기 Type2 LAH 은폐점 후보 선택은 매 반복 전체 거리 재계산 대신 후보별 최근접 거리만 증분 갱신하여 실제 `Scenario_2026-07-28T221304` 전체 8개 임무 dict가 정확히 같은 상태로 388~398ms→48~52ms가 되었다. runtime snapshot·prepared covers·증분 farthest-point의 출력 불변 회귀를 추가했고 전체 modules 테스트 996개를 통과했다.

2026-07-28 v1.5.21 - `Scenario_2026-07-28T223341`의 Type2 다음협업기저 AREA 3갈래 촬영에서 영역 조각 자체는 평행하게 분할됐지만, 각 700m 후속 조각의 T0 진입선(`waypointStartXY -> waypointEndXY`)을 촬영축으로 다시 사용해 UAV6 촬영선이 약 `54° -> 87° -> 130° -> 153°`로 누적 회전하던 문제를 수정함. AREA sweep은 이제 조각별 진입 대각선이 아니라 원본 분할이 공유하는 `bearingDeg`를 사용하고, 촬영 carrier WP도 같은 축 위의 `areaSweepRouteStartXY/EndXY`를 따라 생성함. 진입/선회 경로는 첫 촬영축에 도달하고 실행 방향을 고르는 용도로만 유지하며 실제 촬영축을 바꾸지 않음. 후속 조각 방향 전환과 다음 진입점 계산도 보조 T0 경로가 아니라 실제 촬영 anchor의 시작/종료점을 기준으로 하여, 직전 촬영 종료점에서 가까운 쪽으로 자연스럽게 역방향 진입함. 서로 다른 세 진입각에도 촬영선이 동일 방위를 유지하는 회귀, no-split 실제 sweep 생성 회귀, 촬영 carrier/역방향 hand-over 회귀를 추가하고 관련 다음협업기저 AREA 테스트 132개를 통과함.

2026-07-28 v1.5.20 - `Scenario_2026-07-28T221304`의 두 번째 공격 복귀 계획(700000008→700000010)에서 3분할 Type2 LINE의 활성 UAV 4·5 촬영점 162/130개가 진행률 감시용 2점 중심선으로 교체되어 기존 촬영영역이 사라지고, 전체 156점을 보존한 복귀 UAV 6의 영역만 남아 보이던 문제를 수정함. Type2 개별 소유 LINE은 중심선 진행률을 실제 `lineSearch` 순서의 촬영점 개수로 환산해 이미 촬영한 앞부분만 절단하고, 중심선 자체를 촬영 경로에 덮어쓰지 않도록 분리함. 공격 복귀 UAV에게 촬영 suffix가 남아 있으면 완료 경계 LOITER를 삽입하지 않고 58m/s 비촬영 진입 경로를 생성해 바로 촬영을 재개하며, 촬영 carrier WP가 공격/재계획 위치에 남아 첫 촬영점에서 2km 넘게 떨어진 경우에는 첫 촬영점의 위도·경도와 기존 비행고도를 사용한 할당영역 진입 WP로 교정함. LINE 진행률이 `line_scan_progress`에서 온 경우에도 실제 촬영점 절단이 적용되는 회귀 테스트, 원거리 carrier 교정, 남은 촬영이 있는 복귀 경로의 비-LOITER 계약을 추가하고 관련 회귀 테스트 84개를 통과함.

2026-07-28 v1.5.19 - `Scenario_2026-07-28T221304`의 공격 후 LINE 협업 복귀에서 UAV 6의 첫 촬영 `searchSpeed`가 생성기 산출값 607.40m/s였음에도, 현재 위치에서 할당 촬영점까지의 2,803.1m 비촬영 복귀 거리를 lineSearch 진입 거리로 다시 사용해 110.14m/s로 낮아지던 문제를 수정함. 복귀 UAV는 할당 촬영구역의 비촬영 진입 WP를 별도로 유지하므로, 해당 WP와 함께 생성된 첫 촬영 searchSpeed를 후처리에서 원격 현재 위치 기준으로 재계산하지 않고 그대로 보존한다. 계속 촬영 중인 UAV는 선두 진입 WP를 소비하므로 기존 현재 위치 기준 속도 동기화를 유지하며, 로그와 같은 2.8km 원격 복귀 조건에서도 607.40m/s가 유지되는 것을 회귀 테스트로 검증함.

2026-07-28 v1.5.18 - `Scenario_2026-07-28T215642`에서 UAV 6의 표적 8 격파 후 협업 재분배가 `current-plan progress is already complete`로 실패하면서, 격파 표적 자동추적 LOITER(WP14624)와 촬영 속성이 없고 이미 완료된 복귀 경로(WP14636·14637)를 포함한 기존 공격 패키지 800000042가 그대로 유지되던 문제를 수정함. 협업 재계획 생성 실패 시에도 해당 복귀 UAV마다 return-only fallback을 즉시 생성하여 격파 표적 임무와 과거 복귀 임무를 새 패키지에서 제거하고 tracking assignment를 정상 종료한다. 기록된 복귀 분기에 실행 가능한 촬영점이 없으면 현재 위치의 단일 5초 LOITER만 실행하며, 이후 Input 임무들은 삭제하지 않고 다음 정상 협업까지 실행 차단 상태로 보존한다. 협업 실패 fallback의 MissionPlan 패키지 교체와 실제 로그 구조의 `공격 LOITER + 완료 복귀 경로 + 후속 임무`가 `단일 완료 LOITER + 차단된 후속 임무`로 정규화되는 것을 회귀 테스트로 검증함.

2026-07-28 v1.5.17 - 공격·선행임무 후 복귀 UAV가 다음 협업 재계획으로 잔여 AREA/LINE 촬영을 다시 할당받을 때, 모든 UAV에 공통 적용되던 선두 비촬영 진입 WP 제거 때문에 첫 lineSearch WP가 먼 현재 위치에서부터 촬영을 시작하던 문제를 수정함. 계속 촬영 중인 UAV는 기존처럼 재계획 선두 진입 WP를 소비하지만, 다른 임무에서 복귀하는 UAV는 생성기가 산출한 할당 촬영구역의 `operationMode=1` 진입 WP를 보존하여 해당 구역까지 비촬영으로 이동한 뒤 첫 촬영 WP를 수행하게 함. 촬영 WP가 하나만 남은 경우와 여러 개 남은 경우 모두 복귀 UAV에는 진입 WP가 정확히 하나 유지되고, 활성 UAV의 기존 절단 동작은 유지되는 것을 회귀 테스트로 검증함.

2026-07-28 v1.5.16 - 공격·선행임무·공격 후 복귀에서 잔여 LINE 촬영점이 모두 소진됐는데도 경로 체인을 비우지 않기 위한 fallback이 마지막 fly-by WP를 되살리거나, 협업 복귀 계산이 완료 경로를 중간점+종점의 강제 2-WP 이동으로 다시 덮어써 불필요한 비행이 생기던 문제를 수정함. 원본이 LINE 촬영 경로였으나 실행 가능한 lineSearch 좌표가 더 없으면 일반 이동·복귀 WP를 모두 제거하고 공격/선행임무 종료 위치 또는 현재 위치의 단일 LOITER WP로 정규화하며, `waypointPassType=2`, 촬영 속성 제거, `operationMode=1`, LOITER 5초·반경 180m·속도 30m/s 계약을 공통 적용함. 촬영점이 실제로 남아 있거나 직접 나디르 촬영 경로인 경우에는 기존 복귀·촬영 경로를 보존하고, 먼 과거 복귀 종점·촬영 소진 후 남은 fly-by·공격 계획 생성 시 강제 non-empty resume를 각각 회귀 테스트로 검증함.

2026-07-28 v1.5.15 - 유인기 DEM 경로가 짧고 얕은 지형 굴곡까지 그대로 따라가며 `100→80→100`처럼 불필요하게 하강·재상승하고 고도 전용 WP가 과도하게 남던 동작을 개선함. 기본 30m 이하·1,200m 이하의 짧은 골은 양쪽 안전고도 사이 선형 고도로 들어 올려 평탄화하고, 실제 경로 순항속도와 LAH 운용한계의 75% 상승·하강률을 적용해 필요한 능선 접근은 미리 완만하게 상승하고 통과 뒤에도 급강하하지 않도록 함. 이후 원래 임무 앵커와 수평 우회 굴곡은 보존하면서, 중간 WP를 제거한 3차원 선분이 최대 750m 간격과 모든 20m DEM 샘플의 75m 안전여유를 만족하는 경우에만 고도 전용 WP를 삭제함. 초기계획·공격 재계획·복귀·UAV 지원 연결 경로 모두 실제 속도를 전달하도록 통일했으며, 평탄화 사용 여부·골 깊이·골 길이·중간점 허용오차는 알고리즘 설정에서 조정 가능함. 얕은 골의 `3개→2개 WP` 축약, 필수 능선 보존, 저지대 우회 및 물리 상승·하강률을 회귀 테스트로 검증함.

2026-07-28 v1.5.14 - 다음협업 재계획의 분리된 잔여 AREA 소유영역 병합을 적응형 closing/회랑 방식에서 단순 convex hull 방식으로 교체했다. 테스트 GUI 코드는 import하지 않고 운영 모듈에 직접 구현했으며, raw hull을 그대로 쓰지 않고 `areaAssignmentDetail` 또는 기존 `coordinateList`의 최초/안정 AREA 경계로 clip한 뒤 입력 조각 포함·경계 초과·면적 상한을 재검증한다. 오목한 원래 AREA에서 clip 결과가 분리되면 raw hull로 되돌아가지 않고 원래 단일 경계를 소유권 외피로 사용하므로 결과는 기존 AREA보다 커질 수 없다. 정확한 촬영 의무 geometry와 Type 2/3 branch별 고정 소유권은 그대로 분리 유지한다.

2026-07-28 v1.5.13 - 사천 시뮬레이터 0401 로그 20개(39,186샘플, UAV4~6)의 선회 특성을 분석했다. 지속 선회반경은 중앙값 430m·75% 528m·90% 687m, 선회 진입시간 90%는 2.78초였고, 저장된 다음협업 8초 예측의 실제 위치오차는 90% 82m·최대 87m였다. 자세값은 전부 null이어서 방위·위치 이력 기반 결과를 사용했다. 기존 40m/s 기준반경 450m는 로그 보정배수 1.017로 타당하므로 유지하고, 다음협업 LINE/AREA 진입 계산만 `max(속도 기준반경, 최근 관측반경) × 기존 배수 + 120m`로 보강했다. 순간 관측반경이 작아도 기준값 아래로 줄지 않으며, 완만한 실제 선회는 그대로 반영한다. 120m 여유는 `next_collab_turn_radius_uncertainty_margin_m` 설정과 알고리즘 UI에서 조정 가능하고, 계산 규칙과 통계는 `manual/사천_비행체_선회특성_분석_20260721.md`에 기록했다.

2026-07-28 v1.5.12 - 공격 복귀 재계획에서 이번 close 표적 외에 이전에 이미 격파 확인된 표적의 미완료 공격 행이 원본 계획에 남아 있으면 정상 후보가 `live_attack_continuity_invariant_failed`로 거부되어 같은 close 요청이 재계획 큐를 반복 점유할 수 있던 문제를 수정함. 후보 생성은 targetInfo에 격파된 모든 표적의 공격 임무를 제거하는 반면 연속성 검사는 현재 이벤트 표적 하나만 제거 허용하던 계약 불일치를 바로잡아, 두 단계 모두 현재 표적과 기존 격파 표적 전체를 같은 closed-target 집합으로 사용함. 살아 있는 후속 공격의 IMP·임무·경로·WP 식별자는 계속 엄격히 보존하고, 후보에 격파 표적 공격이 남거나 살아 있는 공격이 사라지는 경우는 기존대로 차단하는 회귀 테스트를 추가함.

2026-07-28 v1.5.11 - AREA 순차 분할의 최대 허용 폭을 800m에서 900m로 상향함. 목표 폭 700m는 유지하고 실제 단계 수를 결정하는 상한만 900m로 조정했으며, 실행 설정·코드 폴백·다음협업 설명 및 폭 분할 회귀 테스트를 동일 기준으로 맞춤.

2026-07-28 v1.5.9 - 다음협업기저임무의 순차 AREA에서 짧게 할당된 영역을 촬영 그룹 2개로 강제 분할하여 WP 간격이 영역 길이의 절반 수준으로 줄고, 첫 촬영 searchSpeed도 그 실제 구간을 따라가지 못하던 문제를 수정함. 실제 산출물에서 총 비행축 약 391m인 AREA가 200m·191m 두 구간으로 강제 분할되고 첫 구간은 비행시간의 6.82배가 촬영에 필요했음. 이제 최소 2그룹 강제를 제거해 짧은 AREA는 `실제 첫 스윕축 점 → 영역 끝 촬영 WP` 한 구간을 그대로 사용하고, 긴 AREA만 `area_wp_interval_m × area_first_packet_sweep_group_scale` 간격에 따라 자연스럽게 여러 촬영 WP로 나눔. 순차 AREA의 간격 누적 기준도 경로 원점이 아니라 실제 첫 스윕축 점으로 맞춰 첫 구간만 짧아지는 현상을 제거했으며, 최종 공개된 모든 촬영 WP마다 실제 직전 WP 거리·해당 WP 비행속도·전체 lineSearch 길이로 searchSpeed를 다시 계산하고 AREA 촬영속도 상한에 따른 비행속도 조정까지 재적용함. 400m 짧은 AREA는 중간 강제점 없이 전체 400m 한 구간, 8km 긴 AREA는 거리 기반 다중 WP로 생성되고 모든 촬영이 각 비행시간의 1/1.10 안에 끝나는 회귀 테스트와 mission_planning 전체 테스트를 통과함.

2026-07-28 v1.5.8 - 현재임무 재수행 뒤 LINE 임무에서 선행임무를 삽입할 때 기존 재수행 폭 조각과 새 협업 잔여 폭 조각이 겹쳐 한 UAV가 전체 폭과 작은 폭을 동시에 촬영하는 것처럼 계획되던 문제를 수정함. 재수행 LINE은 한 UAV에 같은 inputMissionID의 실행 조각이 여러 개 생길 수 있는데, 협업 재분할 성공 뒤에도 선택 UAV의 현재 조각 하나만 선행임무로 교체하고 같은 입력의 나머지 촬영 조각을 남겨 두던 것이 첫 원인이었음. 이제 협업 인계가 성공하면 선택 UAV의 같은 입력 실행 조각을 전부 제거하고 기존 선행임무 이력은 보존한 채 새 선행임무와 촬영 없는 복귀 이동만 삽입함. 또한 LINE 선회 진입 계산 또는 UAV별 IMP 조립에서 일부 조각·일부 UAV만 성공해도 성공분만 새 계획으로 적용하고 실패 UAV에는 이전 폭 임무를 보존하던 부분 교체가 중첩의 두 번째 원인이었으므로, 모든 분할 조각·모든 대상 UAV의 경로와 교체 IMP가 완성된 경우에만 LINE 교체를 허용하도록 원자적 완전성 검증을 추가함. 하나라도 빠지면 어떤 새 LINE 파일도 쓰기 전에 부분 계획을 폐기하고 기존의 비중첩 재수행 할당을 유지하며, 이전 부분 결과 캐시가 재사용되지 않도록 LINE 계획 캐시 버전을 갱신함. 재수행 다중 조각 정리·부분 경로 거부 회귀 테스트를 추가하고 mission_planning 전체 697개 테스트를 통과함.

2026-07-28 v1.5.7 - Type-2 목표지역에서 `현재임무 재수행` 뒤 `다음협업기저임무`를 요청하면 `immutable branch ownership is unavailable`로 계획 생성을 중단하던 문제를 수정함. 원인은 브랜치 소유권을 `DSS_Internal/branch_ownership_state.json` 한 곳에만 저장해 재수행 산출물만 남거나 상태 파일이 유실된 실행에서는 원래 UAV-브랜치 배정을 확인할 방법이 없었던 것임. 다음협업의 두 진입 경로가 공통 복원기를 사용하도록 바꾸고, 상태가 없으면 현재 UAV 위치로 재배정하지 않고 source MissionPlan의 UAV별 IMP에 이미 배정된 target/anchor LINE 중심선 또는 AREA 조각을 원본 InputMissionPlan의 브랜치 형상과 비교해 `branchIndex→UAV`를 복원·재등록한 뒤 계획을 계속 생성함. 모든 live UAV의 배정과 모든 브랜치가 명확히 증명될 때만 복원하며 형상이 모호하거나 일부가 빠진 경우에는 기존 fail-closed 차단을 유지함. 새 0302 산출물에는 `branchIndex`와 AREA 순차분할 계보도 함께 보존해 이후 재수행은 기하 추론 없이 직접 복원할 수 있게 함. 실제 최신 Type-2 산출물의 상태 파일을 비운 검증에서 4번 LINE·5번 AREA·6번 복귀 LINE 모두 기존 `0→UAV6, 1→UAV5, 2→UAV4`를 동일하게 복원했고, LINE/AREA 복원·모호 형상 차단·계보 보존 회귀 테스트를 추가함.

2026-07-28 v1.5.6 - 순차 AREA의 후속 영역에서 out-leg를 없애 달라는 요구와 달리 v1.5.5가 현재 영역의 route start를 비촬영 WP1으로 공개하여 일부 경로에 진입 leg가 남던 문제를 바로잡음. 순차 AREA는 이제 이전 영역 종료점·T′·route start·선회 보조점을 모두 공개 WP에서 제외하고, 첫 촬영축 앵커 자체를 lineSearch 없는 비촬영·주시 WP1으로 전환한 뒤 해당 촬영 데이터는 WP2에 합쳐 `첫점 + 촬영점들`만 남김. 따라서 WP1과 WP2가 같은 촬영 진행축 위에 놓이며 첫점에서 꺾이는 별도 out-leg가 생기지 않고, 후속 영역 WP1은 전역 fly-over 설정에도 fly-by를 유지함. 실제 역방향 순차 AREA에서 인계점과 route start가 모두 제외되고 WP1→WP2가 동일 촬영축을 따르는 회귀 테스트 및 전체 임무계획 테스트를 갱신함.

2026-07-28 v1.5.5 - 다음협업기저임무의 순차 AREA에서 첫 영역 이후 WP1이 직전 영역의 마지막 촬영 WP와 같은 좌표로 복제되고, 전역 fly-over 옵션까지 적용되어 107~169도 급회전을 강제하던 문제를 수정함. 2단계부터는 현재 영역의 실제 비행 방향에 맞춘 route start를 유일한 비촬영·방향정렬 WP1으로 사용하고, 이 점은 고정익 선회가 가능한 fly-by로 보호하여 공개 경로는 계속 `첫 비촬영점 + 촬영점들`만 유지함. 또한 정방향 기준 끝점만 다음 단계에 넘기던 순차 인계 계산을 실제 실행 방향(정방향/역방향) 기준으로 통일하여, 역방향으로 비행한 2단계 뒤의 3단계가 사용하지 않은 반대편 끝점에서 시작하던 불연속도 함께 제거함. 2단계 정렬점·fly-by 보호와 3단계 실제 역방향 종료점 인계를 검증하는 회귀 테스트를 추가함.

2026-07-28 v1.5.4 - 다음협업기저임무 AREA에서 오목하거나 비스듬한 도형의 중앙 단면(midLineLengthM)이 실제 영역 길이보다 짧을 때 WP 경로까지 짧아지던 문제를 수정함. 실제 최신 산출물 UAV4에서 촬영영역의 WP 진행축 길이는 3,323m였지만 공개 WP 경로는 911m(27%)뿐이었고, 이 때문에 다수의 sweep이 짧은 leg에 압축됐음. PATH0와 후속 AREA 경로 모두 중앙 단면뿐 아니라 전체 할당 폴리곤을 비행 진행축에 투영한 길이를 계산하고 그 값을 최소 경로 길이로 사용하도록 통일했으며, 900m 중앙 단면/3,300m 전체 형상의 회귀 테스트를 추가함.

2026-07-28 v1.5.3 - 다음협업기저임무의 순차 분할 AREA 경로가 첫 WP부터 촬영을 시작하던 회귀를 수정함. 700~800m 폭 규칙으로 나뉜 AREA는 긴 T′/out-leg/선회 보조점을 제거하는 후처리에서 lineSearch WP만 남겨, 원래의 `WP1 주시·방향 정렬 → WP2 촬영 시작` 계약까지 함께 삭제하고 있었음. 플래너 보조점은 계속 제외하되 실기체 현재 진입점(후속 조각은 직전 조각의 실제 촬영 종료점)에 operationMode=1·coordinateOrientation을 가진 비촬영 WP 하나를 재생성하고, 첫 lineSearch 촬영 좌표를 바라보도록 연결함. 실제 정상/회귀 산출물의 첫 두 WP를 비교하고 순차 왕복·폭 분할·브랜치 소유권 회귀 116개와 mission_planning 전체 687개 테스트를 통과함.

2026-07-28 v1.5.2 - 임무상태 모니터링 지도의 중첩 가독성을 선택 임무 중심으로 재구성함. 종전에는 현행 입력임무 원본·잔여영역/촬영심도·후보 옵션 할당·최대 8개 완료계획 이력이 같은 무게로 동시에 표시되고, MultiPolygon 조각과 lineList 원소마다 동일 임무 라벨이 반복돼 분할이 많은 계획에서 현재 작업영역을 구분하기 어려웠음. 기본 모드를 `현재`로 두어 동시에 수행 중인 inputMissionID만 강하게 표시하고 다른 현행 임무는 얇은 맥락선으로 낮췄으며, 진행영역·촬영심도·기여경로·계획경로·후보 할당은 집중 임무 ID로 필터링함. 임무 표/지도 라벨 선택 시 `선택` 모드로 전환·해당 영역 확대, `전체`로 현행 전체 복원, 완료 이력은 별도 체크 시에만 지도와 표에 표시하도록 함. 임무구역과 영역 진행 레이어를 분리하고 후보 옵션은 기본 OFF(옵션 탭 선택 시 자동 ON)로 변경했으며, AREA/LINE 라벨은 분할 조각 수와 무관하게 입력임무당 하나만 생성함. FlightPath GeoJSON에도 pathMissionIndex의 inputMissionID/individualMissionID를 전달해 선택 임무의 경로만 정확히 남김. 실제 8개 입력임무·63개 경로 계획에서 현재 모드 라벨 1개, 전체 모드 8개, AREA 선택 M3 단일 강조·확대와 브라우저 오류 0건을 확인했고 mission_status_monitoring 29개 테스트 및 JavaScript 구문 검사를 통과함.

2026-07-27 v1.5.1 - 유인기 공격 브랜치를 목적별 개별임무로 분할함. 기존에는 은폐점까지의 이동이 목적 경로(공격/대기)에 prepend되어 한 경로에 섞여 있었으나, 사용자 요구대로 공격 담당은 [빠르게 숨는 이동] + [은폐→공격→재은폐] 2개, 공격 비담당(지휘기·엄호기)은 [숨는 이동] + [은폐점 대기] + [이후 이동경로] 3개 개별임무로 나눔. 분할 지점은 인증된 은폐점 "직전"이며 은폐점 자체는 목적 임무의 첫 WP로 귀속시킴 — 은폐점을 양쪽에 복제하면 길이 0 레그가 생겨 도착 판정이 안 되는 기존 실패 모드를 재현하게 되므로 공유 WP는 정확히 하나로 유지함. 이동 임무는 individualMissionType=7/patternType=10에 lahCoverIngress=True 마커를 달고 자체 pathID·FlightPath 산출물을 가지며, 종점의 nextWaypointID=0·hovering=0으로 목적 경로 ID 체인에 끼어들지 않게 하고 체류는 전부 뒤따르는 목적 임무가 소유함. 목적 경로 ETA는 _rebase_lah_path_etas로 이동 임무의 종료 시각만큼 이관해 각 레그 소요시간을 보존함(은폐점 진입 레그 시간은 목적 경로가 계속 보유). 대기는 loiter가 아니라 hovering으로 표현함 — loiter는 반경을 도는 것이라 인증된 은폐 셀을 벗어나고 원 반대편은 능선 반대쪽이라 구조적으로 노출됨. 임무 순서는 비행 순서(이동→목적→복귀)를 따르며, v1.4.9에 선행 배치한 lahCoverIngress 관용 가드 3곳이 post_attack 스윕에서 대기임무가 레거시 복귀경로로 오분류되는 회귀를 막음. 검증: 공격기 분할(이동 IM type7·pathID 분리·공격 경로 첫 WP가 은폐점·ETA 재기준·진입 레그 시간 보존)과 지휘기 3분할(이동 체류 0·종점 체인 차단·대기 WP가 인증 은폐점 단독·기본 대기창) 테스트를 새 계약으로 갱신, mission_planning·monitoring·common 724개 전부 통과. 미구현 이월: 공격 실측 소요시간(_lah_attack_window_seconds_from_waypoints)을 비공격기 대기시간에 전달하는 것은 디스크립터 빌더가 병렬 워커에서 각자 ctx 사본으로 실행되어 순서 보장이 없으므로, 지연 기록(_deferredWriteEntries) 후처리 방식이 필요함 — 현재는 기존 기하 추정(_attack_wait_hold_seconds)을 유지함.

2026-07-27 v1.5.0 - 은폐점 안전 2차: 새 적이 발견되면 기존 은폐점을 폐기하고 재인증하도록 하고, 은폐가 통신중계보다 우선하도록 순위를 바로잡음. (A) 공격이 이미 배정된 유인기는 이후 재계획에서 디스크립터를 아예 받지 않아("Reusing committed LAH package unchanged") 은폐점이 낡은 적 목록 기준으로 무기한 재사용됐고, 능선 뒤에 새 적이 나타나면 그 점이 정면 노출임에도 그대로 유지됐음. _preserved_lah_cover_still_masked를 추가해 커밋된 FlightPath 종점을 현재 접촉 집합 전원에 대해 재인증하고, 통과 시에만 패키지를 재사용하며 실패 시 보존을 폐기해 현재 적 기준으로 새 은폐점을 받도록 함(노출보다 공격 연기를 택함). 폐기 시 연속성 계약도 함께 정리(_preserved_source_attack_rows·_preserved_lah_attack_aircraft_ids 해당 기체 행 제거, 그 기체를 앵커로 삼은 _incremental_attack_append 무효화)해 다운스트림 검증이 교체 중인 패키지를 요구하지 않게 함. 적 집합 지문 _enemy_set_fingerprint(1m 반올림·순서무관)를 추가해 재보고 지터와 실제 신규·이동 접촉을 구분함. 증분 append 인증과 커밋 재인증이 갈라지지 않도록 적 마스킹 증명을 _hide_point_masked_from_every_enemy 하나로 통합(거리 게이트 없음·evaluated 요구·고도 미상 거부, 모두 fail-closed). (B) 완전 마스킹과 요구 중계링크를 동시에 만족하는 점이 없을 때의 폴백 정렬이 "UAV 링크 보유"를 1순위, "적에게 안 보임"을 2순위로 두어, 링크 1개를 지키려고 적 3기가 모두 보는 점을 선택했음(지휘기는 tactical_relay_min_uav_links=3이라 이 경로에 상시 진입). 개략(hide_com)·정밀(hide_com_refine) 두 단계 모두 적 가시수를 1순위로 올리고 링크는 동일 은폐 조건 내에서만 최대화하도록 교체하고, 미채점 셀 센티널도 적 우선으로 뒤집음. 검증: 커밋 재인증 8종(3기 중 1기 가시 시 폐기·미추적 광선 폐기·고도미상 광선 미발사·소유권 불일치·경로 판독 실패·지문 4종)과 은폐우선 3종 신설, monitoring·mission_planning·common 715개 전부 통과.

2026-07-27 v1.4.9 - 은폐점이 실제로 적에게 안 보이는지 전면 감사해 fail-open 5건을 차단함. 최대 결함: 적 관측범위를 아군 사거리(tactical_enemy_range_m=5,000m)로 재사용해 그보다 먼 적을 마스킹 분석에서 통째로 제외했고, 실증 기하가 전 기체 적으로부터 5.3~6.3km였으므로 해당 실증의 모든 은폐점이 적 0기 기준으로 "은폐됨" 인증을 받았음(코드 주석이 이 위험을 이미 서술했으나 설정값이 그것보다 작았음). 수정: (1) tactical_enemy_observation_range_m 신설(기본 0=무제한)로 적 관측범위를 사거리에서 완전 분리하고 hide_com 후보평가·hide_com_refine 정밀단계·lah_enemy_contact 경로검증 3곳의 거리 게이트를 제거, (2) 미평가 적 센티널을 +inf에서 -inf로 반전해 광선을 쏘지 못한 적 하나가 은폐 천장을 붕괴시키도록(np.min의 중립원소였기에 미평가 적이 제약을 전혀 못 걸던 fail-open) 하고 적 광선에 reject_nodata=True 적용, (3) 설정된 hide_safety_margin_m=5m이 개략 패스에만 닿고 실제 비행 끝점을 만드는 정밀단계(1.25m)·경로검증(0.25m)에는 누락돼 있던 것을 6.25m/5.25m로 교정(교차단계 간격 1.0 유지), (4) los_api에 evaluated 플래그를 추가해 OUT_OF_RANGE·ENDPOINT_NOT_ABOVE_TERRAIN처럼 지형을 한 번도 샘플링하지 않은 visible=False를 "지형에 막힘"과 구분하고 은폐 인증은 visible is False AND evaluated를 요구, (5) 상속 은폐점 재인증의 3중 fail-open(거리 게이트로 먼 적을 확인됨 처리, 고도 누락 시 0.0m MSL 대입으로 지하에서 전 적 비가시, UAV 링크 nodata 통과) 차단. 함께 개별임무 분할 준비를 무영향으로 선행함: 순수 헬퍼 3종(_split_lah_cover_ingress_waypoints·_rebase_lah_path_etas·_lah_attack_window_seconds_from_waypoints, 레그시간 보존·입력 불변), lahCoverIngress 마커에 대한 다운스트림 분류기 관용 가드 3곳(특히 post_attack에서 이동IM 추가 시 대기IM이 레거시 복귀경로로 오분류돼 스윕에서 살아남는 회귀 차단), 공격 ID 예약량 확대. 검증: 은폐 회귀 테스트 9종 신설(test_hide_point_conceals_from_every_enemy.py)로 각 구멍을 고정, 은폐·전술·monitoring 범위 99개 통과.

2026-07-27 v1.4.8 - v1.4.7 사다리 차폐 대기점이 LINE 계열(직전 기동선 중간·통로 시작/중간)에서 임무 회랑을 벗어나던 문제를 수정함. 원인은 목표지역(AREA) 대기점에만 컨테인먼트를 넘기고 LINE 대기점은 area_list=[]로 호출해 경계 없는 1,500m 원판 탐색이 된 것이며, 실제 계획(사천 8임무 type2, LINE 폭 970~1490m)에서 두 번째 기동선 대기점이 중심선 이격 1,390m(반폭 686m)로 회랑 밖에 배치됨. 수정: _line_corridor_area_rows로 선언된 LINE 폴리라인을 폭만큼 스윕한 회랑 폴리곤(flat cap)을 합성해 컨테인먼트로 전달하고, 앵커 소스 브랜치를 알 수 있도록 _previous_maneuver_line_branch를 분리하고 anchors에 corridorLine(좌표+폭)을 추가함. 컨테인먼트를 필수 조건으로 승격해 폭 미선언 등으로 회랑을 만들 수 없으면 셀렉터를 호출하지 않고 기하 앵커를 유지하며, 셀렉터 클립을 신뢰하지 않고 _point_in_area_rows(ray casting, hole 제외) 사후 검증으로 영역 밖 결과를 기각함. LINE 회랑 대기점은 합성 밴드가 운용자 선언 AREA가 아니므로 _lahTerminalCover* 계약 키를 심지 않아 d0304 재정련 대상에서 제외하고(v1.4.7 이전 동작 유지), 목표지역 대기점만 cover_contract=True로 유지함. 검증: 동일 실계획 재실행 시 회랑 이탈 1,390→284m으로 수용, 전 대기점 컨테인먼트 위반 0건, 지형 이득 유지(+80/+131/+113m), 회랑밴드·hole·폭미선언·영역밖기각 테스트 4종 추가로 602개 전부 통과.

2026-07-27 v1.4.7 - 유인기(type2/3) 사다리 대기점을 기하 중심에서 지형 차폐점으로 전환함. 직전 협업기동 line 중간·통로 시작/중간·목표지역 centroid 앵커를 그대로 기준으로 삼되, ground_maneuver_mode에 _lah_cover_hold_info를 신설해 기존 select_lah_terminal_cover_point(DEM 1배치, 후보 25·광선 48)로 반경 1500m(lah_cover_search_radius_m) 내에서 "무인기가 작업 중인 다음 임무 기하(centroid+정점 최대 5점)를 등지고 능선 뒤 저지대"에 앉는 점을 선택함. 안전장치: 위협 방향으로 앵커 거리의 60% 미만까지 전진하면 기각(한 다리 뒤 의미 보존), 목표지역 내 대기는 원판∩영역+경계이격 컨테인먼트 유지 및 _lahTerminalCover* 계약 키를 심어 d0304 UAV ETA LOS 재정련까지 연결, 실패·비활성 시 기하 앵커 그대로. 노브 lah_ladder_cover_enabled(기본 1)·lah_cover_search_radius_m(기본 1500)을 attack_mission 스키마에 등록하고 LRU 캐시(64)로 재계획 반복 호출을 흡수함. 실지형(지포) 검증: previous_mid 지면 367→198m(800m 이동, 0.08초), 목표지역 내부 440→221m(계약 키 탑재, 0.02초), 캐시 재호출 2ms. 테스트: 사다리 차폐 6종 신설(위협 샘플·전진 기각·실패 폴백·컨테인먼트 계약·토글·캐시), conftest에서 테스트 기본 OFF 고정으로 기존 기하 검증 유지 — 598개 전부 통과.

2026-07-27 v1.4.6 - 유인기 저고도 경로탐색 2차 성능 개선으로 운용 기본값(strength 1.0) 기준 레그당 0.85→0.28초(최초 6.6초 대비 약 24배, 품질은 평균 저감 121m/길이 1.30x로 동일 유지). 프로파일로 확인된 지배 비용을 제거함: (1) 격자 노드마다 중심선 전체 호장을 재계산하던 _offset_along_polyline 호출(하버사인 170만회)을 _polyline_stations 1회 순회로 대체해 스테이션별 기준점·수직단위벡터를 미리 계산, (2) DP 완화마다 경로 히스토리 리스트를 복사하던 것을 부모 포인터+역추적으로 교체, (3) 사전 프로브 요청을 직선 통계와 같은 provider 배치로 병합(운용 terrain_elev_many는 호출당 타일 인덱스 오버헤드가 있어 배치 수 자체가 비용), (4) 재중심 패스를 실측 기준 2→1회로 캡(2회는 품질 동일·시간 +60%: 121.2 vs 120.2m). DEM provider 요청점도 89k→61k로 감소. 회귀: 인제 3레그 피크 전부 직선 이하 유지, strength 0~3 단조, mission_planning 스위트 586개 통과(스위트 43→26초).

2026-07-27 v1.4.5 - 유인기 저고도 경로탐색을 재설계해 레그당 계산시간을 1/3 이하로 단축함(인제 실지형 3레그, 동일 측정환경: strength 2.0 기준 6.6→1.75초/레그 3.8배, 운용 기본값 1.0 기준 0.85초/레그 7.8배). 속도 개선 5건: (1) 길이 가중 사다리를 예산 첫 적합에서 조기중단(피크 가중별 최대 8회→평균 1~2회 DP), (2) 재중심 패스를 strength와 무관하게 상수 2회로 캡하고 개선폭 3m 미만이면 즉시 수렴 처리, (3) 간선 지형 샘플 4점→2점(중점+도착노드, 양자화 dedup로 손실 미미), (4) 간선 구축을 도착차선 인덱스 조회로 바꿔 O(차선²)→O(차선×스텝), (5) 사전 프로브 12점으로 주변에 유의미한 저지대가 없으면(15m 미만) 격자 생성 자체를 생략하고 strength 해석을 레그당→경로당 1회로 축소(runtime deepcopy 제거). 효과 보완: strength 1.0 초과 시 피크 허용치를 (strength-1)×30m 완화해 낮은 안장을 넘어 더 깊은 계곡으로 진입 가능하게 함(1.0 이하는 기존 게이트 그대로) — 단 실측상 인제 3레그는 회랑 내 최저 계곡에 이미 도달해 저감이 ~124m에서 포화하므로, 시간 대비 이득이 없는 운용 기본값을 2.0→1.0으로 되돌림(평균 저감 120m/길이 1.31x/피크 전부 직선 이하, 다이얼은 0~3 유지). 안장 통과 회귀 테스트를 추가하고 mission_planning 스위트 564개 전부 통과(스위트 시간 92→43초).

2026-07-27 v1.4.4 - 유인기 저고도 우회를 기본으로 더 강하게 적용함: uav_params values에 lah_low_terrain_strength=2.0을 설정하고(코드 기본 1.0, 0=off·최대 3) 키를 runtime_settings DEFAULT_RUNTIME_VALUES 스키마에 등록함(canonicalize가 미등록 키를 걸러내므로 등록 없인 파일 값이 무시됨). strength 상향에 필요한 가드 2건을 보완함: (1) 기본값(2,400m)보다 좁게 요청된 회랑은 d0304 임무 LINE 폭 계약이므로 strength가 절대 넓히지 못하게 클램프, (2) 회랑이 넓어질 때 차선을 자동 증차해 ~100m 간격을 유지(선회각 불변, _LOW_TERRAIN_MAX_HALF_LANES=60 백스톱)하고 좁은 회랑은 기존 세밀 간격 유지. 테스트는 conftest autouse로 다이얼을 1.0에 고정해 운용 파일 값에 좌우되지 않게 하고, 좁은회랑 불확장·강도별 단조·클램프/기본값 테스트 3종을 추가함. 인제 실지형 3레그 실측(strength 2.0): 평균 지형고도 저감 120→124m(레그1 157→170m), 경로길이 1.30x→1.36x, 최대고도 전부 직선 이하, 길이예산 상한 2.8x로 확대되어 보상이 큰 지형에서 추가 우회 여지 확보. mission_planning 테스트 563개 전부 통과.

2026-07-27 v1.4.3 - 알고리즘 설정 3종(uav_params·replan_settings·replan_settings_defaults)을 settings/에서 형상관리 대상인 modules/resource/로 이관하고 settings_paths에 algo_settings_file 자동 마이그레이션(레거시 사본 보존)을 추가함, settings/에는 PC 고유 파일만 남김. 사용처가 없는 MDSControl 모듈과 대시보드 "임무 계획 설정 SW" 버튼·런처·QSS를 전부 제거함. 유인기 저고도 경로탐색을 강화함: 회랑 2,400m(차선 49개로 100m 간격 유지)·길이예산 1.9x·평균지형 가중 16·피크 가중 사다리 600까지 확장하고, 이전 승자 경로를 중심선으로 회랑을 재구축하는 재중심 패스(REFINE_PASSES=2, 호장 기반 _offset_along_polyline)를 도입하되 원 레그 기준 cross-track이 요청 회랑을 넘지 않도록 격자를 고정해 임무 LINE 폭 계약을 유지함. 도착노드 기준 incoming 간선 그룹화로 탐색을 O(차선×스텝)로 최적화하고 uav_params values의 lah_low_terrain_strength(0=off, 기본 1.0, 최대 3)로 강도를 조절함. 검증: 인제 실지형 3개 레그 평균 지형고도 저감 100→120m(레그별 최대 157m)·경로길이 1.20x→1.30x·최대고도는 전부 직선 이하, strength 0/0.5/1.5 단조 동작, mission_planning 테스트 560개 전부 통과(구버전 튜닝 하드코딩 테스트 2건은 계약 상수 기준으로 갱신), 버전 테스트는 change_log 첫 줄 파싱 기준으로 전환.

2026-07-27 v1.4.2 - AI 에이전트가 이 코드를 수정할 때 따라야 할 규칙을 `modules/수정규칙.md`로 명문화함. 버전 기록의 실제 수정 위치가 루트 `run.py`가 아니라 `modules/change_log.md` 첫 줄(단일 진실 원천)이며 `version_info.py`→`modules/run.py` GUI 표기와 시나리오 `DSS_KU_VERSION.json`이 자동 파생됨을 명시하고, 파서 정규식·패치 +0.0.1 규칙·파생값 및 fallback 상수 수정 금지를 못박음. DEM은 `resource/`의 Inje_10m·Hongik_48km·Jipo_48km 3종만 사용하되 경로 하드코딩 대신 `common/regional_dem.py` 레지스트리를 경유하고 SRTM 1arc 타일로 조용히 폴백하지 않도록 규정함. ICD는 `docs/reference/MessageLibrary_건대송부`를 기준으로 삼고 generator/receiver가 자동 생성물이라 수동 수정이 재생성 시 소실됨을 경고함. 문서 전용 변경이며 실행 코드 동작 변화 없음.

2026-07-25 v1.4.1 - 지상작전부대(type=2)의 각자도생 LINE→AREA→복귀 LINE을 하나의 불변 브랜치 세트로 고정함. 최초 `branch_index→aircraftIDs`를 시나리오 단일 진실 원천으로 잠그고 초기계획 재실행·다음협업 LINE 현재위치 재배정·캐시 복원에서도 덮어쓰지 않으며, 영역 수가 UAV보다 적으면 같은 영역의 최초 공동 소유자만 내부 분할하도록 보장함. 공격·선행임무는 일시 이탈로 처리해 다른 UAV에게 잔여 촬영을 재분배하지 않고 복귀 시 자기 잔여 경로를 이어가며, 공격/선행 종료 재결합과 경로이탈·일반 remaining fallback도 전체 pool 재분할을 차단함. AREA 소유자 누락·손상 시 타 영역 UAV fallback 대신 기존 계획을 유지하고, LINE split/plan 캐시에 소유권과 브랜치 순서를 포함함. 단일 영역(N=1)도 지원하되 뒤 ACP/통제권변경 LINE은 브랜치 세트에서 제외함. 회귀검증: 3 UAV/2 AREA의 소유 그룹 [1,2], LINE·AREA·복귀 동일 소유, 위치 교환 및 재실행 불변, 캐시 왕복, Type2 공격 preserve/Type1 redivide 통과.

2026-07-24 v1.4.0 - 2026-07-24 최종 수정본을 정식 1.4.0 기준선으로 지정함. 새 `Logs/Scenario_*` 생성 시 시나리오 루트에 `DSS_KU_VERSION.json`을 원자적으로 기록하여 실행 코드 날짜·버전·기록시각·시나리오 식별자를 보존하고, `modules/change_log.md` 첫 유효 항목을 런처·대시보드·시나리오 로그가 함께 사용하는 단일 버전 기준으로 통합함. 버전 로그 기록 실패는 임무 시나리오 활성화를 중단하지 않고 경고만 남기도록 처리함.

2026-07-22 v1.3.81 - 재계획 MissionPlan이 이전 계획을 복제하면서 최초 `planningTime`을 계속 상속하던 문제를 수정함. 0902 수신 시 고정한 요청별 고해상도 시각을 기준으로 0301 직전에는 복제값을 현재 요청 경과시간으로 교체하고, 실제 0305 `missionPlanningStatus=2` 송신 성공 콜백 직후 해당 요청에서 전달된 모든 MissionPlan에 정확한 `0902_received_to_0305_status_2_sent` 값을 ms 단위로 원자적 반영함. 연속 요청은 inflight context로 분리하고 한 요청의 복수 선택안만 같은 시간을 공유하며, 실패·재계획 불필요처럼 새 계획을 전달하지 않은 완료는 기존 파일을 갱신하지 않음. Log Analyzer는 `DSS_Internal/replan_timing_history.json`의 실측값을 모든 `resultMissionPlanIDs`에 매핑해 우선 표시하고 이력이 없는 과거 로그만 MissionPlan 값을 사용함.

2026-07-22 v1.3.80 - 유인기 전투진지·목표지역 대기 종말점을 AREA 중앙/공격좌표에 고정하지 않고, `dem_cover_analysis`의 후보 샘플링·지형 LOS 개념을 경량화하여 AREA 내부 저지대·위협 차폐·ETA 기준 UAV LOS를 함께 평가하도록 개선함. 후보는 Outer-Hole과 경계 안쪽 최대 100m 범위에 강제하고, ACP/기존 공격좌표는 실패 시 fallback 및 soft 선호로만 사용함. 후단 UAV 20km 보정도 AREA/Hole 제약을 지키며, AREA 안에서 20km를 만족할 수 없으면 영역 밖으로 이동시키지 않고 내부 엄폐점을 유지한 채 최대속도로 접근하도록 처리함. 다중 표적 cycle별 전투진지/표적 형상을 분리해 전달하며 DEM 실패 시 무중단 내부 fallback을 적용함.

2026-07-22 v1.3.79 - 유인기 저지대 경로가 LINE/AREA 경계를 벗어날 수 있던 문제를 수정함. LAH 특수임무 변환에서 원본 LINE의 `lineList/width`와 AREA/Hole 제약을 보존하고, 형상 정보가 없는 좌표 임무에서는 무제한 측방 저지대 우회를 만들지 않도록 안전 폴백을 적용함. LINE 저지대 후보는 선언 반폭의 80% 이내로 제한하고 AREA는 외곽·Hole에서 폭의 10%(최대 100m)를 이격하여 실제 비행·좌표 반올림 여유를 확보함. 최신 Scenario_2026-07-22T000944의 폭 1,000m LINE 재생 결과 최대 중심선 이격을 기존 1,100.2m에서 346.4m로 줄이고 경계 위반 13/19건을 0건으로 확인함.

2026-07-21 v1.3.78 - 유인기 임무계획의 각 임무 마지막 대기점을 계획 ETA 기준으로 대응 무인기(LAH1-UAV4, LAH2-UAV5, LAH3-UAV6)의 예상 위치와 비교해 수평거리 20km 이내가 되도록 보정함. UAV 0303 누적 ETA 경로를 선형 보간하고, 제한 초과 시 2km 여유를 둔 18km 지점으로 마지막 구간을 지형추종 경로로 재생성하며 다음 유인기 임무 시작점도 새 종점에 연속되게 연결함. 이미 20km 이내인 경로와 carry-forward immutable 경로는 유지하고, 공격 동작이 있는 종점은 공격점을 보존한 뒤 지원 대기점으로 이동하도록 처리함. 계획 시작점부터 UAV와 20km를 초과하는 시험 조건에서는 도달 불가 오류를 내지 않고 해당 유인기 경로 속도를 운용한계 최대속도로 올려 가능한 한 빨리 보정 위치로 이동하게 함.

2026-07-21 v1.3.75 - 임무계획·공격/엄폐 분석·랜덤임무·SIM이 동일한 운용 DEM 3종(`Hongik_48km.tif`, `Inje_10m.tif`, `Jipo_48km.tif`, 모두 EPSG:32652)과 실제 WGS84 경계를 공유하도록 공용 레지스트리를 추가하고, 공용 DEM 선택/배치조회·LAH RL 경로·SIM terrain/카메라 footprint가 종전 `n37_e...` 저해상도 타일이나 디렉터리 전체 스캔으로 폴백하지 않도록 통일함. GeoTIFF PixelIsArea tiepoint에 잘못 적용되던 반 픽셀 이동을 제거해 원본 rasterio 픽셀과 고도 샘플을 정합했으며, SIM의 LAH1~3 지도 라벨에 최신 생성 비행계획 활성구간 기준 수평 이탈거리와 동일 DEM 기준 지면 이격고도를 상시 표시하고 DEM 범위 밖은 명시적으로 경고하도록 함. 세 DEM 메타/샘플/선택과 SIM 진단 필드 회귀테스트를 추가함.

2026-07-13 v1.3.73 - 대기갑항공타격작전(type=1)의 DEM 리뷰 후 복귀 순서를 변경함. 10개 임무 중 8번 협업기동임무를 목표지역→공격대기지역(regionType=4)에서 목표지역→전투진지(regionType=5)로 변경하고, 9번 ACP 복귀선도 공격대기지역 시작이 아니라 8번 전투진지 도착점에서 시작하도록 연결함. Type 1 LAH 특수 시퀀스 패턴과 8·9번 행동/폴백 앵커도 target_to_battle, battle_to_acp 기준으로 정합했으며 원본 6개 입력 가이드와 다른 package type은 변경하지 않음.
2026-07-09 v1.3.72 - 도넛 순찰(type=4) 촬영 중 공간해상도 모니터가 "스윕대기"로 멈춰 풋프린트/등가 GSD/만족률이 전혀 집계되지 않던 문제를 수정함(Scenario_2026-07-09T035914 실증). 원인 2중: ① 다음협업 도넛 재계획(`_prepare_donut_replacements`)이 mission_info를 처음부터 조립하면서 SEP/FOV/SPEED를 누락함 — 일반 재계획은 template(직전 임무 info) 기반이라 보존되는데 도넛만 빠졌고(초기계획 SEP=674 vs 재계획 SEP=None), ② 공간해상도/촬영품질 모니터의 `_apply_mission_plan_view`가 SEP 없는 임무를 통째로 스킵해 그 임무 WP 메타가 아예 안 만들어져 스윕 게이트(`_is_sweep_measurement_active`)의 has_line_search 판정이 영구 False였음(0401은 mode=2·filming=1로 정상 송신 중이었음). 수정: ① 재계획 도넛 mission_info에 직전 밴드 임무의 FOV/SEP/SPEED/BEARING을 이월(`_apply_donut_capture_fields`, sticky 밴드라 기하 불변)하고, template에도 없으면(수정 전 재계획 위에 재계획) wplist에서 유도 — FOV=스윕행 fieldOfView, SPEED=행 speed m/s×3.6(km/h 계약), SEP=링 leg 평균 길이(스테이션 간격=방사 스윕 간격). ② 모니터는 SEP 없는 임무도 WP 메타를 만들되 SEP 기반 임계값 계산만 비활성화(None-safe 확인). 검증: 실제 시나리오 데이터로 template 이월(SEP=674 유지)·무template 유도(SEP=876/FOV=7.2/SPEED=178) 정상, 수정된 게이트 시뮬레이션에서 WP11086(orient 전이)=닫힘·WP11087+(스윕, filming=1)=열림·filming=2(비행완료, ICD상 비촬영)=닫힘, type2/3/4/5·재할당 회귀 통과. 참고: 0401 filming enum은 0=None/1=촬영중/2=비행완료라 filming=2 구간 미집계는 정상 동작임.
2026-07-09 v1.3.71 - 도넛 순찰(type=4) 카메라 DEM 고도 조회(v1.3.69)로 초기임무계획이 느려진 성능 회귀를 수정함. 원인: v1.3.68에서 도넛 패킷을 d0303 densify 후처리(배치 DEM)에서 제외한 뒤 v1.3.69가 촬영점 DEM을 좌표 1개씩 단건 조회(`_dem_alt`/`terrain_elev`)로 넣었는데, 단건 경로는 호출마다 캐시 시그니처 확인+타일 resolve 오버헤드를 물어 실측 약 1.84ms/pt(warm도 0.47ms/pt)이고 도넛 lineSearch는 기체당 촬영점이 수백 개라 밴드 3개 기준 약 2.2초가 추가됐음(배치는 0.01ms/pt, 약 150배 차). 수정: `build_donut_wplist`가 wplist에 필요한 모든 DEM 좌표(촬영점 전부+WP 좌표)를 모아 `_resolve_ground_alt_batch`로 한 번에 해소하고 per-point fn은 배치 미해소분 폴백으로만 사용함 — d0303은 자체 캐시 배치 `_dem_alt_many`를 `camera_altitude_many_fn`으로 주입(런맵/메트릭 유지, WP 좌표 선배치로 `_mission_wp_alt`의 단건 조회도 warm 히트화), 다음협업 기본 경로는 공용 `terrain_elev_many` 배치+`_DEM_GROUND_CACHE` 워밍. 검증: 촬영점 378개 전수 단건 vs 배치 고도값 완전 일치(양 경로 모두), 배치/단건 wplist JSON 동일, 콜드 3밴드 wplist 빌드 2.22s→0.32s(잔여는 프로세스당 1회 타일 오픈), t4 e2e 고도 불변·type2/3/4/5·재할당 회귀 통과.
2026-07-09 v1.3.70 - 도넛 순찰(type=4)의 첫 WP가 아직 도달하지도 않은 촬영점을 "저 멀리서" 미리 찍던 문제를 수정함(Area 임무 WP 촬영 계약 정합). 원인: 도넛 wplist가 departure-packing이라 WP[i]의 lineSearch가 그 WP에서 *출발하는* 스윕(leg i→i+1)을 담았고, SIM은 "WP의 lineSearch는 그 WP로 *가는 도중* 촬영"하는 계약이므로, WP0가 담은 첫 스윕이 테이크오버 지점→링 진입까지의 긴 전이 구간 내내 촬영돼 링 도달 전 원거리에서 찍혀버림. 원래 Area 임무는 첫 WP=촬영지점을 쳐다보는 좌표고정 orient(operationMode=1, coordinateOrientation), 두 번째 WP부터 "가면서 촬영"(lineSearch)하는 구조임. 수정: `build_donut_wplist`를 arrival-packing으로 재작성해 WP[i](i≥1)가 직전 WP[i-1]의 스윕 lineSearch를 담고(=그 leg를 실제로 나는 도중 촬영), WP0는 스윕을 비우고 첫 스윕 시작좌표(DEM 지면고도)를 향한 `operationMode=1`+`coordinateOrientation.coordinate` orient WP로 방출함. 플래너 터미널 WP는 원래 스윕을 소유하지 않는 FLYOVER 복귀 노드라 시프트로 유실되는 촬영 leg가 없음(검증: film leg 8개 전부 보존). 초기계획(d0303)·다음협업 재계획 모두 같은 `build_donut_wplist`를 거치므로 양쪽에 자동 적용되고, 카메라 좌표 DEM 고도(v1.3.69)·WP 비행 고도(지면DEM+레이어)는 그대로 유지됨. 검증: fixture와 d0303 e2e에서 3밴드 전부 WP0=OPMODE_POINT(lineSearch 없음)·WP1+=OPMODE_LINE, WP1 lineSearch=옛 WP0 스윕(좌표수 일치), orient 좌표=첫 스윕 시작점·고도=DEM, type2/3/4/5·재할당 회귀 통과.
2026-07-09 v1.3.69 - 도넛 순찰(type=4)의 lineSearch(카메라 촬영점) 좌표 고도가 지면 DEM이 아니라 비행 고도로 들어가던 문제를 수정함. v1.3.68에서 도넛 패킷을 d0303 스윕 후처리에서 제외하면서, 그 후처리가 하던 촬영점 DEM 고도 재산정(v1.3.30 계약: target/lineSearch 고도는 비행체 고도와 분리해 지면 DEM 기준)까지 건너뛰어, 촬영점이 기체 비행 고도(예: 1020)를 그대로 갖게 됐음(카메라 지향점이 지면이 아닌 비행층에 있어 footprint/렌더 왜곡). 수정: `build_donut_wplist`에 `camera_altitude_fn`을 추가해 각 lineSearch 좌표 고도를 해당 지점 지면 DEM으로 세팅함(초기계획은 d0303의 캐시 `_dem_alt` 주입, 다음협업은 기본 공용 DEM 캐시). WP 좌표 고도는 기존대로 비행 고도(지면 DEM+레이어)이며, 다음협업의 WP 비행 고도도 레이어만 쓰던 것을 지면 DEM+레이어로 맞춰 초기계획과 동일 AGL로 정합함. 검증: 초기계획·다음협업 모두 WP 비행고도=지면DEM+레이어, 카메라 촬영점=지면DEM(실측 DEM과 일치), type2/3/4/5·재할당 회귀 통과.
2026-07-09 v1.3.68 - 도넛 순찰(type=4) 초기임무계획에서만 카메라가 멀리 찍히고 스윕선이 "일부 가로 일부 세로"로 어긋나던 문제를 수정함(다음협업 재계획은 도넛 플래너 출력을 그대로 써서 정상이었음). 원인: d0303의 area/line 스윕 후처리들이 도넛의 완성된 방사 zigzag lineSearch를 area 스윕 스트립으로 오인해 변형함 — (a) `_densify_linesearch_strips_inplace`가 각 청크를 `strip[0]→strip[-1]` 직선으로 뭉개 카메라가 밴드를 가로지르는 긴 직선을 훑고(멀리 찍힘), (b) 패킷별 route/sweep 재배치 루프(엔트리 오프셋·스윕 재정렬·인접 line 중간선 ㄹ자 삽입)가 좌표를 추가하면서 interpolationPoints를 안 맞춰, SIM이 lineSearch를 interpolationPoints 단위로 청킹할 때 방사 스윕을 가로질러 잘못 연결함. 수정: 두 후처리 모두 `_donut_patrol` 마커 패킷을 건너뛰게 함(formation follower와 동일 취급) — 도넛은 방사 zigzag가 이미 완성돼 있으므로 재보간/재배치 불필요. waypointID 할당·DEM 고도·정규화는 그대로 적용. 검증: 초기계획 도넛 lineSearch가 다음협업과 좌표수·interpolationPoints까지 완전 일치(coordCount%interpolationPoints=0), 카메라 경로 총길이 동일·최대 세그먼트 방사 스윕 폭 수준, 0302 영역은 정식 환형(v1.3.66) 유지. 고도값은 원래 정상(DEM+레이어)이었고 촬영좌표 청킹이 문제였음.
2026-07-09 v1.3.67 - 도넛 순찰(type=4)의 다음협업기저임무 재계획에서 동심 밴드(양파껍질) 소유가 깨지고 환형이 이상하게 재분할되던 문제를 수정함. 종전에는 도넛 경계임무 재계획이 area 경로(`_prepare_area_replacements`)로 흘러 일반 division 플래너가 환형(외곽-구멍)을 하나의 컴포넌트로 받아 기하적으로 재분할해, 초기계획의 밴드 배정과 무관한 이상한 형태가 나왔음. 수정: next_collab 디스패치에 도넛 감지(`_is_donut_replan_mission`: packageType=4 + isHole 경계)를 추가하고 전용 `_prepare_donut_replacements`로 라우팅함 — 저장된 sticky 밴드 소유(branch_ownership)를 `ownership_override`로 고정해 각 무인기가 자기 밴드만 도넛 플래너로 재계획하고(소유 없으면 현재 위치로 재앵커·저장), 밴드는 정식 환형 `areaList=[{isHole:F,외곽},{isHole:T,내곽}]`+`_donutPatrol` 마커로 방출, FlightPath는 도넛 링 경로/방사 lineSearch wplist를 공용 ID 예약·waypoint 할당 경로로 조립함. 두 진입점(prepare/run) 모두 formation 다음·line 앞에 도넛 분기를 넣음. 검증: 위치를 뒤섞어도 sticky 밴드 소유(0→6,1→4,2→5) 불변, 무인기별 환형+마커+연결된 waypointID 방출, type2/3/4/5·재할당·순서 회귀 전부 통과.
2026-07-09 v1.3.66 - 도넛 순찰(type=4) 밴드가 모니터링/SIM에서 "반쪽"으로 보이던 표시 버그를 수정함. 원인: 0302 표시용 밴드 폴리곤을 환형(ring)인데도 단일 좌표열 `outer + inner[::-1]`(bowtie)로 내보내, 형상/좌표에 따라 자기교차가 발생하고 SIM/모니터링의 fill-rule(even-odd/nonzero)이 자기교차 폴리곤을 반만 채워 렌더링함. 계획·경로·카메라 커버리지 자체는 100%였고(플래너는 프로토타입과 완전 일치, 정사각/가늘고긴/오목/편심구멍/얇은환형/2·1대 전부 100% 검증됨) 순전히 표시 폴리곤 문제였음. 수정: `band_rings_latlon`로 밴드의 외곽/내곽 링을 분리해 각 밴드 조각이 `coordinateList`=외곽(유효 단순폴리곤)+`_donutBandInner`=내곽을 들고, 0302 조립(`_piece_to_mission_info`)이 도넛 밴드를 `areaList=[{isHole:false, 외곽}, {isHole:true, 내곽}]` 정식 환형으로 방출하도록 함 — 이제 밴드가 반쪽이 아닌 온전한 링으로 렌더링됨. d0303 도넛 경로/카메라(마커 기반)는 불변. 검증: 3밴드 전부 render-valid 환형, e2e 및 type2/3/5·재할당 회귀 통과.
2026-07-09 v1.3.65 - 항공지원작전-중요시설 방호(type=4)를 추가함. ① 도넛 순찰 알고리즘을 test/donut_patrol_sim에서 `modules/mission_planning/planners/donut_patrol/`로 이식함(순수 로직, GUI 제외, test 폴더 비의존): 링 루프 경로+방사 lineSearch(operationMode=2) 분리, capture 법칙 직결(FOV DB 선택/간격·속도 통일), 수직이격 Δθ×r 스테이션 배치, 코너당 WP 2개+300m 병합 등 프로토타입 확정 설계 유지. ② 협업경계(mtype=3)/경계지역(regionType=7)에서 areaList에 외곽+isHole이 함께 오면 도넛 임무로 감지하고, 초기계획 split이 무인기별 환형 밴드 조각(branchIndex 태그+`_donutPatrol` 마커)으로 분할함 — 밴드=각자도생 소유(밴드당 1대, 표적 발견 시 담당 유지, 종료 후 자기 밴드 복귀), 소유는 branch_ownership 스토어에 저장하고 재계획은 저장 소유로 밴드 재구성(밴드 이동 없음). ③ 0302 info에 마커를 패스스루하고 d0303가 마커 감지 시 일반 area 스윕 대신 도넛 플래너의 링 경로+방사 lineSearch wplist를 방출함(waypointID 공용 후처리 할당, DEM 기준 고도, 실패 시 일반 area 경로 폴백). ④ 유인기 시퀀스는 type5와 동일한 staged attack-wait 흐름을 공유(트리거를 도시(11)+경계(7)로 확장): 1~2번 통제권 출발점 대기, 첫 경계 임무부터 공격대기지역 대기, ACP#2·통제권변경 추종(+ACSGCS 후속). ⑤ area 다음협업 재계획 게이트를 pkg4까지 확장하되 isHole 임무는 원소=브랜치 순서 재구성을 건너뛰고 잔여 스냅샷의 sourceAircraftID로만 소유를 유지함. 검증: 7임무 합성 시나리오 전체 파이프라인(21조각/6패키지, 밴드 1대1 소유 저장), d0303 도넛 패킷 3개(WP ID 할당·링크, film WP, DEM 고도), LAH 7단계, type2/3/5·재할당 회귀 전부 통과. (도넛 fresh-areaList 다음협업 재계획의 밴드 재구성, 공격 재계획의 잔여 area 재분배 skip은 후속)
2026-07-09 v1.3.64 - 각자도생(type=2/3) 브랜치 소유의 재할당-추종 규칙을 확정 반영함(사용자 확정). 종전에는 다음협업기저임무 재계획이 항상 초기임무계획 때 저장된 브랜치 소유에 고정되거나, 위치 기반 재배정이 일어나도 스토어가 갱신되지 않아 이후 임무(경계 area/목표·착륙 line)가 옛 소유를 따라가 line-area-line 세트가 깨질 수 있었음. 새 규칙: ① span 진입 앵커인 경계(regionType=7) LINE 임무의 다음협업 재계획은 저장된 소유를 무시하고 무인기 현재 위치 기준으로 브랜치 소유를 재유도하며(prefer_fresh), 최종 piece 배정 결과를 `_persist_branch_line_reassignment`가 스토어에 덮어씀(전체 plan 캐시 히트 경로 포함). ② 그 외 브랜치 임무 — 경계 area(단일 재계획에서 앵커처럼 감지돼도 kind=area면 재유도 금지), 목표(6)/착륙(9) line — 는 항상 최신 스토어 소유를 sticky 추종함. 스토어에 span 메타(branch_mission_ids/anchor)를 보존한 채 소유만 교체하는 `update_branch_ownership`를 추가함. 검증: 초기 0→4/1→5/2→6 저장 후 앵커 line 재계획(위치 스왑)이 0→6/1→5/2→4로 재유도·커밋되고, 이후 M5 area·M6 line 재계획이 (기하를 뒤섞어도) 새 소유를 그대로 추종, M5 단일 재계획은 재유도하지 않음. 기존 sticky 회귀(앵커 재계획 없으면 초기 소유 유지)·type2/3 계약·type5 전부 무회귀.
2026-07-09 v1.3.63 - 도시지역 작전(type=5)을 추가함. 무인기 측은 각자도생 브랜치 없이 기존 협업 폭분할 파이프라인을 그대로 사용하며(브랜치 게이트는 type 2/3 유지), type_decider의 BASE_PACKAGE_TYPES에 5를 포함해 협업수색공격(mtype2)·협업도심수색공격(mtype6) area가 구역수색(individualMissionType=3/pattern6), line이 6/8로 명시 매핑되도록 함. 유인기(LAH) 시퀀스는 regionType 기반 신규 빌더(`build_urban_operation_lah_sequence`)로 처리: 첫 도시지역(regionType=11) 임무 이전=통제권변경 출발점 대기(1~2번), 도시 구간부터=공격대기지역(regionType=4, area 중심 우선/線 종점 폴백) 대기(3~5번), ACP(3)=공격대기 대기 후 무인기 후미 추종 ACP#2(6번), 통제권변경(2)=ACP#2 대기 후 종점 추종(7번, 도착 후 ACSGCS 통제권변경은 메시지 후속). 0302 조립은 type1 특수→type2/3 지상기동/공중강습→type5 도시→일반 순 폴백 체인. 유인기는 도시지역에 진입하지 않으며 표적 발견 시 편대기 접근공격/복귀는 기존 공격 재계획 기본 동작을 사용(목적지 공격 앵커 없음). type=5도 0204 없이 바로 0902 초기임무재계획(V1.3.61/62 게이트 자동 적용). 검증: 7임무 합성 시나리오 전체 파이프라인(21조각/6패키지), LAH 7단계 시퀀스, type2/3 무회귀.
2026-07-09 v1.3.62 - MMR(임무계획)의 0204 자동 발신 경로를 제거해 0204(협업기저임무계획) 송신 주체를 MSM으로 일원화함. 종전에는 MMR이 0301 송신 성공 직후 활성 계획 컨텍스트의 inputMissionPackageID로 0204를 자동 발신했고, type=1 리뷰 도입 시 이 발신을 제거하는 대신 "MSM이 선송신한(리뷰된) ID는 스킵"하는 중복 방지만 얹었기 때문에, 리뷰가 없는 모든 패키지(type=2/3/4/5)는 여전히 MMR이 0204를 발신하고 있었음(사용자 관측: type=2 packageID=3에서 0204 발신). 수정: MMR의 post-0301 0204 발신 블록과 관련 헬퍼(`_push_0204_payload`/`_normalize_0204_payload`/`_resolve_0204_input_package_id`)를 제거함. 이제 0204는 MSM이 type=1 0201 리뷰 흐름에서 0902에 앞서 선송신하는 경우에만 존재하며, 그 외 type은 0204 없이 바로 0902로 진행함. MMR의 0204 수신 핸들러(`_on_output_plan_0204`)와 0902 선송신 마커 파싱은 MSM 발신 이력 trace 용도로 유지함(송신에는 미사용). CSC 송수신 표도 새 구조에 맞춤: MSM 탭(mission_monitoring_tab)은 발신 목록에 0204를 추가하고 수신 목록에서 제거(리뷰 선송신의 `_find_tx_row("0204")`가 이제 행을 찾아 발신 완료 표시), MMR 탭(assignment_planning_tab)은 발신 목록에서 0204를 제거하고 수신만 유지함. 정보관리/결심지원 탭의 0204는 원래 수신 전용이라 변경 없음.
2026-07-09 v1.3.61 - MSM의 type=1 전용 0204 리뷰 선송신이 다른 InputMissionPackageType 시나리오에서도 발동되던 문제를 수정함. 원인: 리뷰 대상 "최신 0201" 선택이 InputMissionPlan 디렉토리의 최대 파일 ID 기준이라, 이전 type=1 시나리오가 남긴 원본/리뷰 산출물(원본ID+1, `inputMissionPackageType=1` 유지)이 현재 시나리오의 0201보다 큰 ID로 남아 있으면 그 낡은 type=1 파일이 리뷰 대상으로 잡혀 0204가 재송신되고(재기동 시 리뷰 산출물을 다시 리뷰해 2011→2012→2013… 체인), 0902가 낡은 type=1 계획 ID를 가리킬 수 있었음. 수정: ① 리뷰 산출물에 provenance 마커(`reviewSource="MSM"`, `reviewedFromInputMissionPackageID`)를 각인하고 리뷰 picker가 마커 파일을 항상 제외(재리뷰 체인 차단), ② 0201 수신 시 해당 packageID/payload를 앵커로 기록해 리뷰는 "실제 수신한 현재 시나리오의 0201"만 대상으로 삼음(파일 미기록 레이스 시 메모리 payload 사용), ③ 재기동 폴백은 최대 ID가 아니라 mtime 최신의 비-리뷰 파일 기준. 결과: type=1 0201 수신 시에만 리뷰→0204→0902(신규 ID) 흐름이 동작하고, 그 외 type은 리뷰/0204 없이 바로 0902 초기임무재계획을 요청함. 검증: RX 앵커/재기동 mtime 폴백/type1 정상동작/마커 스킵/레이스 경로 5케이스 통과.
2026-07-08 v1.3.60 - 각자도생(type=2/3) area 다음협업기저임무 재계획의 브랜치 순서 보장. area 컴포넌트를 unary_union으로 만들면 원소 순서가 뒤섞여(예: 우/좌/중), 직전 보정의 centroid 매칭이 대개는 맞지만 순서 보장이 아니었음. inputMissionType 4번(경계 line)·5번(경계 area)·6번(목표 line)의 lineList/areaList는 반드시 원소 index k가 동일 무인기에 세트(line-area-line)로 가야 하므로(사용자 확정), 브랜치 area 재계획은 `_branch_area_components_in_order`로 areaList 원소를 순서 그대로 컴포넌트화하고 각 컴포넌트에 `ownerAircraftIDs`(=저장된 소유 index k)를 명시해 배정함. `_branch_area_owner_for_component`는 이 명시 소유를 최우선 사용(모니터링 잔여영역 스냅샷은 `sourceAircraftID`, 그 외 centroid는 최후 폴백). 모니터링 잔여영역(areaSegmentList) 재계획은 세그먼트별 소유를 그대로 유지. 검증: 사용자 실제 0201 mission4/5/6로 초기계획 및 순서 컴포넌트 모두 index0=좌→무인기A, 1=중→B, 2=우→C 세트 일치.
2026-07-08 v1.3.59 - 각자도생(type=2/3) 다음협업기저임무 재계획 두 가지 보정. (1) area 재계획을 브랜치-aware로 수정: 경계지역 area를 무인기 수로 재분할하던 것을 각 area 컴포넌트가 sticky 소유 무인기에게만 배정되도록 함(`_prepare_area_replacements`). 소유 판별은 잔여영역 스냅샷의 `sourceAircraftID` 우선, 없으면 areaList 원소 중심점을 브랜치에 매칭해 스토어 소유 조회하며, 단일 소유면 area 통째·복수 소유면 그들끼리만 분할함. (2) line WP 2000m 그룹핑의 짧은 꼬리 미병합 수정: `_resample_polyline_xy`가 항상 종점을 별도 route point로 덧붙여 마지막 구간이 stub(예: 2000+100)으로 남던 것을, 마지막 스팬 미만 잔여를 직전 full 구간에 흡수하도록 병합함(확정 area-sweep 꼬리 규칙 1-2 full + 짧은 2-3 → 1-3 병합과 동일). 이제 마지막 구간은 2000~4000m 사이로만 생기고 stub이 없음. line 경로 전반(각자도생 포함)에 적용됨. 또한 도형 타입 계약 변경(line=1,7 / area=2,3,4,5,6; type4/5=area 정찰)에 맞춰 `ground_maneuver_mode.py` 분류가 정합함을 확인함.
2026-07-08 v1.3.58 - 공중강습작전부대 엄호 작전(type=3)을 type=2와 동일한 각자도생 브랜치 구조로 확장하되, 코드가 두 타입을 regionType/packageType으로 명확히 구분하도록 구성함. 무인기 각자도생 분할·sticky 소유·다음협업 재계획 로직은 type=2와 완전히 공유하며 게이트만 `inputMissionPackageType∈{2,3}`으로 넓힘(경계 앵커·브랜치 span·소유 스토어 동일). 차이점은 유인기(LAH) 앵커·시퀀스에서만 처리함: type=3은 앞단에 탑재지대(regionType=8) 이동/대기 단계가 추가되고, 엄호 목적지·유인기 대기·공격 앵커 기준 영역이 목표지역(6)이 아니라 착륙지대(9)임. 유인기 통합 규칙 — 탑재지대(8)=이동, ACP#1 도달 전(=ACP#1 임무까지)=대기점(type3 탑재지대/type2 통제권변경 출발점) 대기, 경계 이전=ACP#1 대기, 경계(7)=ACP#1→목적지 등분 엄호이동, 목표/착륙(6/9)=목적지 대기, 이후 ACP(3)=ACP#2 추종, 통제권변경(2)=종점 추종(type3은 도착 후 ACSGCS 통제권변경). 공격 특화도 목적지 hold 임무(경계 이후 첫 regionType 6 또는 9)에서만 목적지 수렴점으로 앵커링하도록 일반화함. type=1 특수작전과 단일원소·기타 패키지는 종전대로 무영향. (경계 표적발견 시 편대기 우선 급접근 대상 선정, 각자도생 area next_collab 재계획은 type2와 공통으로 후속)
2026-07-08 v1.3.57 - 지상작전부대 기동여건 보장 작전(type=2) 유인기(LAH) 시퀀스와 목표지역 공격 앵커를 추가함. 유인기 3대는 각자도생 분할을 타지 않고 공통 앵커를 따라 대기/엄호이동/추종함: 1번 통제권변경지역 출발점 대기, 2~3번 ACP#1 대기, 4~5번 경계 구간에는 ACP#1→목표 종점(수렴점)을 임무 수만큼 등분해 점진적으로 엄호 이동, 6번 목표지역 대기, 7번 ACP#2·8번 통제권변경 종점으로 이동(무인기 후미 추종은 기존 0304 LAH-UAV ETA 동기화가 담당). 앵커는 regionType(ACP=3/목표=6/경계=7/통제권=2)에서 해석하며 목표지역 중심은 목표 line 종점들의 수렴점을 사용(사용자 확정). 0302 조립은 type=1 특수시퀀스(build_lah_special_sequence)가 우선, 없으면 type=2 시퀀스, 그 다음 일반 per-mission LAH로 폴백함. 공격 특화는 유인기가 목표지역에 체류하는 목표 hold 임무(경계 이후 첫 regionType=6, 예: 6번)에서만 공격 좌표를 목표지역 수렴점으로 앵커링하고, 그 이전/이후 임무는 각 기체 현재 위치 기준 주변 공격점을 그대로 사용함. 유인기 1=지휘기·2/3=편대기 역할을 프로파일에 기록(사용자 확정). type=1 특수작전 공격 앵커(전투진지)는 종전대로 우선 적용되며, 프로파일은 InputMissionPackageType=2에서만 생성돼 다른 패키지·기체(무인기)는 무영향. (경계 표적 발견 시 편대기 우선 급접근/지휘기 근방 복귀의 대상 선정 우선순위, 각자도생 area next_collab 재계획은 후속)
2026-07-08 v1.3.56 - 지상작전부대 기동여건 보장 작전(type=2) 무인기 "각자도생" 분기 로직을 추가함. `inputMissionPackageType=2`에서 경계지역(regionType=7) lineList/areaList가 2개 이상인 임무를 각자도생 앵커로 잡아, 기존처럼 한 line/area를 무인기 수만큼 폭분할하는 대신 list 원소 하나=브랜치 하나를 무인기 1대가 통째로 맡도록 분할 방식을 전환함(무인기 수>브랜치 수면 남는 무인기가 앞 브랜치부터 세부 분할, 무인기 수<브랜치 수면 근접 무인기가 복수 브랜치 담당). 앵커에서 정한 `브랜치index→무인기` 소유를 브랜치 span(경계 area·목표 line 등 원소 수가 같은 연속 임무, 예: 4~6번) 내내 고정하며, 소유 맵은 `InputMissionPackageID` 기준으로 `DSS_Internal/branch_ownership_state.json`에 저장해 초기계획과 이후 다음협업기저임무 재계획이 같은 소유를 재사용하도록 함(적 발견 시 담당 무인기 고정, 다른 무인기가 대신 수행하지 않음). 다음협업 단일임무 재계획에서는 임무 자체 regionType만으로 브랜치 여부를 알 수 없는 목표지역(regionType=6) 후속 임무도 저장된 브랜치 임무ID 목록으로 인식해 예측거리 기반 재배정 대신 저장된 소유로 확정 배정함. 단일 원소 임무(ACP/통제권변경 등 재수렴 구간)와 type=1/3/4/5 등 다른 패키지는 종전 폭분할·거리기반 배정을 그대로 유지함. (type=2 유인기 시퀀스/공격 특화는 후속 반영 예정)
2026-07-08 v1.3.55 - 대기갑항공타격작전(type=1) DEM 리뷰 결과의 recommended position을 전투진지 `battleAttackCoordinate`로 0204 리뷰 payload에 보존하고, LAH 공격 재계획의 특수 공격 앵커가 이 좌표를 우선 사용하도록 연결함. 공격 앵커 고도는 기존 LAH 특수작전 기준과 동일하게 유지하며, 좌표가 없을 경우 기존 전투진지 area 중심으로 fallback함.
2026-07-08 v1.3.54 - 대기갑항공타격작전(type=1) 0204 리뷰 결과의 10개 입력임무 패턴에 대해 LAH 초기계획 특수 시퀀스를 분리함. 1번은 통제권변경지역 출발점 대기, 2~3번은 ACP#1 대기, 4~5번은 공격대기지역 대기, 6~7번은 전투진지 대기/공격 앵커로 매핑하고, 8번은 목표지역->공격대기지역, 9번은 공격대기지역->ACP#2, 10번은 ACP#2->통제권변경지역 line을 각각 독립 추종하도록 정리함. 목표 이후 line을 하나로 병합하던 기존 egress 처리는 이 패턴에서 사용하지 않고, 8~10번 LAH line에는 endpoint 보존 플래그를 붙여 0304 생성 시 가운데 구간 trimming 없이 실제 도착점까지 유지되도록 보정함.
2026-07-08 v1.3.53 - 대기갑항공타격 작전(type=1) 초기임무재계획 진입 전 0201 리뷰 흐름을 MSM으로 이관하고, 6개 입력 임무를 DEM 기반 전투진지 포함 10개 임무로 재구성하도록 보강함. MSM은 0101 mode=2 수신 시 최신 0201의 `inputMissionPackageType=1`과 지정 입력 패턴만 대상으로 삼아, 목표지역을 적 출현 영역으로, 두 번째 ACP line의 종점을 REF로 사용해 `dem_cover_analysis` 순수 알고리즘을 실행하고, 추천 영역을 `inputMissionType=2/regionType=5` 전투진지 area로 삽입함. 공격대기지역->전투진지, 전투진지->목표지역, 목표지역->공격대기지역 복귀, 공격대기지역->ACP 연결 line을 생성하되 line `regionType`은 도착 region 기준으로 부여하고, 새 `InputMissionPackageID` payload를 `InputMissionPlan/{id}.json`에 저장한 뒤 0204(`협업기저임무계획`)를 `source=MSM`으로 선송신함. 이후 0902 `replanRequest="초기임무재계획"`에는 새 `inputMissionPackageID`와 0204 선송신 마커를 포함하며, MMR은 초기임무재계획 STEP 0에서 0902 컨텍스트의 `inputMissionPackageID`를 신뢰해 `InputMissionPlan/{id}.json`을 읽고 0204 수신 ID는 후속 0204 중복 발신 방지에만 사용함. 다른 `InputMissionPackageType`에는 적용하지 않음.
2026-07-05 v1.3.52 - 다음협업 첫 WP 고도 입력(진입 고도) 오염 수정. "첫 WP 고도=0401 고정" 정책 자체는 동작 중이었으나, 모니터링의 0803 execute 진입 좌표 생성에서 기본(midpoint) 전략이 고도를 (0401+다음 진입점)/2로 평균 내고, 참조 델타 보정이 목표 진입 고도를 대입해 정책 입력이 이미 오염돼 있었음(Scenario_2026-07-05T190552: 0401 1338/1780~2088 vs 전달 고도 1419/1502~1587 — 첫 WP가 기체 고도와 최대 501m 어긋나 임무 시작부터 급강하 지시 발생). 진입 좌표의 lat/lon 전략(현재위치/중간점/다음시작)은 유지하되 고도는 항상 0401 실측을 쓰도록 `_resolve_entry_altitude`를 도입하고, 0401 고도가 비정상(결측·700m 미만)일 때만 종전 참조 보정으로 폴백함.
2026-07-05 v1.3.51 - 다음협업 area WP 구간 꼬리 병합 수정. area 스윕 WP 그룹핑(_group_area_sweep_items_by_spacing)에서 밀도 상한(64개=피치 22m 기준 약 1421m)이 목표 간격(uav_wp_interval_m×area_first_packet_sweep_group_scale, 현 1800m)보다 먼저 걸려 간격 파라미터가 무력화되고, 잔여 꼬리 병합이 80개/16개 휴리스틱에 걸려 거부되면서 짧은 꼬리 WP(예: 로그 AC5 645m)가 남던 문제를 수정함. 밀도 상한을 좌표 폭주 방지 수준(256)으로 완화해 간격이 분할을 지배하게 하고, 목표 간격에 못 미치는 마지막 구간은 이전 그룹에 흡수(1-2 간격 + 2-3 잔여 → 1-3 병합, 사용자 확정 규칙)하도록 단순화함. 좌표 총량은 WP당 ICD 상한(2000)과 v1.3.50 densify의 상한 로직이 계속 보장함.
2026-07-05 v1.3.50 - Area 촬영점 간격 상한 도입. Scenario_2026-07-05T190552 로그 분석 결과 area 스윕 스트립(단면 스캔선)은 고정 개수(sweep_line_interp_points=3)로만 보간되어 촬영점 간격이 단면 폭에 비례(좁은 첫 단면 3~8m, 넓은 단면 830m+)해 "첫 간격은 짧고 뒤로 갈수록 길어지는" 불균일이 확인됨(line은 폭 333m 일정이라 균일하게 보였을 뿐 같은 구조). 진행 방향 스캔 피치(22~24m)와 FOV/SEP 선택은 캡처 법칙대로 동작 중이었음. 0303 패킷 조립 직전에 lineSearch별 최장 스트립 기준으로 보간 개수를 올려 촬영점 간격을 `sweep_interp_max_gap_m`(기본 500m, GUI `Sweep 점 최대 간격`, 0=끔) 이하로 균일화하는 densify 패스를 추가함. interpolationPoints 청킹 계약(스트립당 균일 개수)과 WP당 좌표 총량 상한(2000)을 유지하며, 좁은 스트립·불규칙 배열은 변경하지 않음.
2026-07-05 v1.3.49 - 다음협업기저임무 WP 고도 상승률 정합. 상승률 제약의 역방향 인상 패스가 잔여 상승을 진입 직후 첫 레그에 몰아넣어(감속 완화는 WP1 한 곳뿐) 달성 불가능한 고도 점프가 생기던 문제를 수정함. 상승/강하 용량을 전이 WP의 감속 가능 바닥 속도(최소 전진속도) 기준으로 계산해 선행 WP 인상을 줄이고, 최종 고도차 기준으로 급상승/급강하 레그의 전이 WP 속도를 실제로 낮추는 속도 패스를 추가함(촬영 WP 속도는 capture 법칙 계약이라 불변). SIM UAV에 수직속도 envelope(max_climb_rate_mps=5.0/max_descent_rate_mps=7.0, uav_dynamics_profile.json 조정 가능)를 추가해 종전 pitch 한계 기준 ~25 m/s까지 상승하던 것을 임무계획 가정 상승률과 일치시킴 — 상승 불가능한 계획이 사내 sim에서는 통과해 보이던 간극을 제거함.
2026-07-05 v1.3.48 - line/area 촬영 진행영역 적립 게이트 강화. 0401 센서 운용모드(2=구간탐색/구역감시)만 커버리지 적립을 허용하는 공통 게이트(`capture_gate.py`)를 신설해 좌표지향모드 원거리 사전 주시와 선회 중 우발 관측으로 임무영역이 잘못 소거되던 문제를 차단함. 게이트는 filming/flying 상태, 센서중심-기체 지상거리(기본 1500m, `KU_CAPTURE_GATE_MAX_SENSOR_OFFSET_M`), 계획 폭 대비 풋프린트 폭 배율(기본 3.0배, `KU_CAPTURE_GATE_FOOTPRINT_WIDTH_RATIO`)을 함께 검사하며, 모드 필드가 없는 레거시 피드는 기존 동작을 유지함. 적용 지점: 진행률 트래커 면적 적립(swept 브리지 차단 포함)·센서 기반 sweep point 정밀 전진, line 스윕 구간 적립, 진행영역 스냅샷 모니터의 행 샘플링/풋프린트 행 확증/계획선 커밋/궤적 소거(종전 filming 미확인 경로 포함). 0401 워커 전달 필드에 sensor_operation_mode/flight_mode를 추가하고, 플랜 전환 직전 잔여 스냅샷을 강제 저장해 재계획 carry-forward가 오래된(더 넓은) 잔여를 읽어 기촬영 영역이 되살아나는 여지를 줄임. SIM 잔여영역 시각화(`/api/sim/remaining_areas`)에 line 임무를 추가해 잔여 line 구간을 폭 반영 회랑(리본) 투명 폴리곤 + 중심선으로 area와 동일한 스타일로 표시하며, 담당 기체가 1대인 임무는 해당 기체 색상으로 렌더링함.
2026-06-08 v1.3.47 - 공격/area 재계획 후속 수정. 공격 임무 생성 시 area 입력을 기존 UAV 배정 유지 대상으로 건너뛰던 guard를 제거해 표적 추적 UAV 1대가 빠진 뒤 남은 area가 잔여 UAV들에게 다시 재분배되도록 복구함. LAH 공격 후 resume path에서 공격점 인근 transition waypoint가 특수작전 공격대기 anchor로 복귀하면서도 공격점 고도(300m)를 유지해 지면 쪽에 박혀 보이던 문제를 resume anchor 고도 기준으로 보정하고, 비공격 LAH waypoint의 weaponType을 0으로 정리함. 영역/sweep 진행분 절단과 협업 잔여 재계획의 전방 예측 시간을 9초에서 5초로 낮춰 과도한 선행 절단을 줄임.
2026-06-07 v1.3.46 - 당일 통합 수정. InputMission regionType을 ACP/공격대기지역/전투진지/목표지역 흐름에 맞춰 정리하고, 다음협업기저 재계획에서 area polygon 꼭지점이 LAH 경로점으로 복사되어 꼬인 선 경로가 생기던 문제를 area 중심 앵커 1점으로 보정함. 공격대기지역->전투진지->목표지역 세트가 들어온 경우에만 LAH가 무인기보다 한 단계 늦게 추종하고 공격대기지역에 체류하며, 공격 시 전투진지점에서 사격 후 공격대기지역으로 복귀하도록 초기 계획/next-collab/공격 재계획 경로를 연결함.
2026-06-05 v1.3.43 - 당일 점검 반영. 모니터링/로그 검증을 보강하고 다음협업기저 DB 선택 공통화, sweep 밀도 적용 확인, Log Analyzer footprint 패널 위치 조정을 반영함.
2026-06-05 v1.3.42 - run.py 대시보드 정리. 임무계획 모듈 구조 정리/검토 문서를 보강하고, 메인 화면에 공간해상도 모니터를 연결했으며 카드·입력·스핀박스·IP 선택창·안내 메시지 QSS를 밝은 공통 템플릿으로 통일함.
2026-06-05 v1.3.41 - 당일 통합 수정. 무인기 동역학 로그 기반 sim 보정값과 임무계획 선회 여유 배수를 정리하고, 현재임무 재수행은 현재 진입부만 재생성하고 후속 임무를 carry-forward하는 fast path로 단축함. 다음협업기저임무 line/area FOV 및 T'/T0 진입 여유를 조정했으며, 부분 path 생성 실패 시 기존 target 임무가 삭제되어 53112 missionSegmentID가 빠지지 않도록 보존 guard를 추가함. 입력 임무 중복 좌표 정리와 공간해상도 모니터 누적 처리 부담 점검을 반영하고, 메인 화면 최근 업데이트 날짜를 26-06-05로 갱신함.
2026-05-21 v1.3.31 - 당일 통합 수정. 메인 대시보드에서 모듈 서비스 상태 문구 표시, 개발관리 버튼, Change Log 전용 표 창, Quick Actions 배치를 정리해 변경 이력 확인성을 보강함. 현재임무 재수행 재계획은 옵션 후보 병렬 실행을 적용하되 후보별 입력/FlightPath/결과 병합 경계를 분리해 정찰특화와 일반 옵션 결과가 꼬이지 않도록 보강하고 처리 시간을 줄임. 촬영 계획 고도 산정에서는 lineSearch 지형 최대고도 계산의 중복 DEM 샘플링을 제거하고 `d0303.py` 내부 DEM/groundRequired 캐시를 추가해 촘촘한 sweep에서도 coordinateList/FOV/SEP 데이터 개수는 유지한 채 고도 보정 계산량을 줄임. 모니터링 장시간 운용 중 `Qt5Core.dll` 종료 로그는 이벤트 로그와 모듈 로그 기준으로 원인을 추적하고 PyQt UI thread 접근/고주기 갱신 위험 지점을 점검함.
2026-05-14 v1.3.30 - 촬영 계획의 target/lineSearch/areaSearch 고도를 비행체 고도와 분리해 DEM 기준으로 재산정하고, 촬영점 고도가 WP 비행고도보다 높아지는 경우 WP 고도를 최소 안전 여유만큼 보정하도록 공통 guard를 추가함. 인제 지역은 `resource/Inje_10m.tif`를 EPSG:32652 지역 DEM으로 우선 사용하도록 DEM 선택 로직을 확장했으며, 선택된 DEM 파일명/좌표/고도 샘플이 `DSS_Internal/dem_usage.jsonl`에 남도록 진단 로그를 추가함. area 촬영계획의 DB 최대 SEP 대신 운용 파라미터로 촬영 segment 상한을 조정할 수 있도록 GUI/runtime 연결을 보강하고, 메인 화면 최근 업데이트 날짜를 26-05-14로 갱신함.
2026-05-11 v1.3.29 - 당일 통합 수정. 0501 currentInputMissionID 롤백 방지 및 heartbeat/queued 0501 stale payload 폐기 보강; 0803 현재임무 재수행 시 0201/InputMissionPlan 복제와 현재 협업기저임무 복제/삽입 처리 추가; 다음 협업기저임무 line 재계획의 촬영 준비점/진입 방향/현재 위치 기반 분할을 보정하고 편대비행 재계획 경로를 추가; 공격 후 복귀 재계획에서 복귀 UAV와 임무 완료 UAV의 마지막 hold/잔여 WP를 보존하고 후속 input mission 시각화가 사라지지 않도록 보강; 경로 미추종 재계획이 현재 input mission 밖의 다음 임무 WP를 잡지 않도록 currentInputMissionID 경계 검사를 추가; sim input 경계 waypoint에서 0803 없이 다음 협업기저임무로 자동 진행되지 않도록 boundary loiter 처리를 수정; post-attack 단일 잔여 WP의 searchSpeed를 현재 UAV-WP 거리 기반으로 재산정하고 배수 적용을 조정; 정찰특화/area 선회 연결이 임무계획 알고리즘 설정의 공통 Dubins 절대 선회반경을 따르도록 정리; line 진행영역 관리에서 순간적인 FOV/line 추적 끊김으로 남은 line이 뒤로 풀리지 않도록 provisional frontier sticky 보정을 추가; 0902 sidecar 저장을 고유 tmp/retry 방식으로 보강해 배속 시 다음협업기저임무 요청이 수신 후 처리 예약 전 누락되는 상황을 방지; 60 byte 표시 제한에 맞춰 주요 GUI/notice/재계획 사유 문구를 축약하고 다음 협업기저임무 전환 사유에 `_` prefix를 적용; 메인 화면 최근 업데이트 날짜를 26-05-11로 갱신함.
2026-04-29 v1.3.28 - 0401 기반 비가용 재계획을 보강하여 통신두절/무인기 고장/임무장비 고장이 발생 후 55초간 지속될 때 health 값과 별개로 비가용 처리 후 재계획하도록 정리하고, 동시 발생 시 재계획 사유 우선순위를 통신두절 > 무인기 고장 > 임무장비 고장 순으로 고정함. 기존 RTB 비가용 판단 및 재계획 경로는 유지하면서, 선행임무 인가/협업 잔여 재계획/타 UAV 복귀 갱신 후보 선정에서도 VehicleStatus 비가용 및 RTB UAV를 제외하도록 보강하고, 선행임무 요청/후보 선정이 스킵되는 경우 원인 로그를 남기도록 진단성을 보강함. 또한 current-remaining 재계획의 기존 잔여 임무 순서/ID 정합성을 맞추고, 0301 이후 0305/0901/0903 송신 전 대기와 ID 관계 탭 갱신 블로킹을 줄여 재계획 결과 송신 지연을 완화함.
2026-04-24 v1.3.27 - 공격 후 복귀 재계획에서 활성 UAV 평균 진행률이 70% 이상이라 협업 재합류를 스킵하는 경우에도 그냥 종료하지 않고, 현재 남은 임무만 기준으로 active UAV들의 IMP/FlightPath를 다시 생성해 지나온 prefix mission/path를 버린 채 남은 2대가 분담 수행하도록 보강함. 동시에 표적 자동추적 UAV는 tracking branch를 제거하고 마지막 복귀점으로 향하는 return-only path(58m/s)와 후속 mission만 남긴 새 IMP로 교체되며, closed tracking assignment도 함께 정상 종료되도록 정리함.
2026-04-06 v1.3.26 - 일반 0201 재계획(0401/0802 한정)의 current remaining hybrid 경로를 정리하여 monitoring 진행영역 snapshot으로 합쳐진 현재 남은 line/area 임무를 next-collab line/area planner로 재생성하고, 나머지 후속 임무는 기존 초기 임무 재계획 흐름(run_divide_and_pattern + 0303/0304) 그대로 유지하도록 보강함. 병렬/순차 일반 재계획 경로 모두 동일한 current-hybrid 적용 지점을 사용하도록 맞췄고, 0802 hold 만료 지연 재계획에도 currentRemainingCollaborativeReplan / entryAircraftList / turnRadiusScale 문맥이 붙도록 monitoring payload augmentation을 확장했으며, fresh 0201 입력 갱신형 재계획은 기존처럼 제외함.
2026-04-06 v1.3.25 - 다음 협업기저 임무 line 재계획을 area와 유사한 구조로 확장하여 선회 반영 entry/heading 기반 진입점 계산, line piece 재분할/재할당, 전용 line path/sweep 생성을 지원하도록 추가 보강함. corridor sweep 전개 방향과 route anchor를 기존 line scan mode에 맞게 다듬고, raw sweep spacing과 grouped lineSearch waypoint 병합 로직을 정리하여 여러 sweep이 하나의 WP에 묶이도록 수정했으며, GUI/runtime의 line density 및 route offset 배수가 해당 재계획 경로에도 직접 반영되도록 연결함.
2026-04-03 v1.3.24 - 임무계획 line 밀도 배수가 corridor sweep 간격에 직접 반영되도록 수정하고 route WP 간격과 분리함. line/area 밀도 배수 상한을 10.0으로 확대했으며, hold->entry 구간에서 search entry point 고도가 다음 sweep 고도로 급강하하던 문제에 상승/하강률 제한을 추가해 비현실적인 고도 점프를 막음. run.py / run_offline.bat 실행 차이로 섞이던 UI import 경로도 정리해 동일한 화면과 동작을 사용하도록 고정함.
2026-04-03 v1.3.23 - 임무 진행영역 관리의 소거 로직을 전면 개선함. 기존 개별 cut-line strip + bridge 방식에서 boundary cut line 기준 반평면 차감 방식으로 교체하여, 곡선 경로 구간에서 strip 간 빈틈(초승달 모양 미소거 영역)이 반복 발생하던 문제를 해소함. 이제 진행 방향 기준으로 boundary 뒤쪽 영역은 전부 소거 완료로 처리됨.
2026-04-02 v1.3.22 - 다음 협업기저임무의 FOV 해석을 수정해 PATH0 / assignment-path 행이 최종 `resolvedFovDeg`로 entry-T' SEP 휴리스틱 행을 재사용하지 않도록 함. 이제 파이프라인이 sweep builder와 동일한 width/SEP 호환 DB 기준으로 FOV/속도를 해석하므로, DB 기반 sweep planning이 활성화된 동안 1.7° 같은 작은 값으로 잘못 축소되지 않고 `fov_db.csv`의 큰 가용 FOV 값을 복원함.
2026-04-02 v1.3.21 - Done/resume sweep 분할이 이제 sweep trimming에만 추가 ~3초 lookahead를 적용함. `sweep_progress.json`에 명시적 `buffer_seconds`가 없을 때 임무계획 재계획 사용처(attack/prior/imaging)는 `elapsed_seconds`, `planned_seconds`, `sweep_point_count`, `seconds_per_point`로 버퍼된 sweep point를 추정하므로, mission ID, path ID, non-sweep branching logic은 바꾸지 않은 채 resume path가 몇 개의 sweep point 앞에서 잘리도록 보정함.
2026-04-02 v1.3.20 - Attack UAV resume trimming을 비공격 UAV resume split과 정렬함. 공격 재계획이 탐지 UAV에서 과도하게 크게 잘린 sweep을 보면, 이제 전체 미절단 branch를 복원하는 대신 `progress_points` 기반 trimming으로 fallback하여 추적 UAV가 attack-exclude/prior 재계획과 동일한 부분 trim sweep waypoint 구조로 재개하도록 함.
2026-04-02 v1.3.18 - 공격 임무 생성 전 과정의 실패 사유를 `0001` NoticeInfo로 통보하도록 보강함. 공격 특화 파이프라인과 일반 공격 옵션 적용 경로 모두에서 `가용 유인기 없음`, `잔여 탄약(type1~3) 부족`, `표적/공격기 좌표 누락`, `기준 MissionPlan 로드 실패`, `공격 산출물 생성 실패` 같은 케이스를 사람 읽을 수 있는 문구로 정리해 발송하며, 특히 잔여 탄약이 모두 부족한 경우 공격 임무를 생성하지 않고 바로 안내 메시지를 내보내도록 수정함.
2026-04-02 v1.3.17 - 공격 임무의 무기 선택에 실시간 0401 탄약 재고(`type1/type2/type3`)를 반영함. 표적 type으로 먼저 선호 `weaponType`을 정한 뒤, 선택된 공격기의 남은 탄약을 확인하여 동일한 우선순위(예: 유도탄 우선 시 `2→1→3`) 안에서 실제 발사 가능한 무기로 자동 보정하고, 적용된 재고/선택 결과를 공격 로그와 메타데이터에 함께 남기도록 보강함.
2026-04-02 v1.3.16 - 공격 임무가 `0402 targetType`과 GUI 설정을 기준으로 자동 무기를 선택하도록 확장함. 공격 표적 후보는 `표적 우선순위(target_type_priority)` 순서대로 선택하고, 선택된 표적의 `targetType`별 `weaponType` 매핑(`weapon_for_target_type_1~6`)을 적용하며, GUI에서 fallback 무기와 표적별 기본 무기를 함께 조정할 수 있도록 연결함. 기존처럼 명시적 0402 표적이 들어오면 그 표적을 우선 사용하고, targetType 정보가 없을 때만 fallback 무기를 사용함.
2026-04-02 v1.3.15 - 공격 임무의 `weaponType` 해석을 ICD 기준으로 고정해 `1=기관포, 2=유도탄, 3=로켓` 의미를 코드와 GUI에 반영하고, GUI 입력 범위와 runtime 정규화를 `0~3`으로 제한함. 현재 공격 임무는 여전히 전역 `weapon_type` 설정값을 사용하지만, ICD 범위를 벗어난 값은 저장/적용 단계에서 자동 보정되도록 정리함.
2026-04-02 v1.3.14 - 임무계획 알고리즘 파라미터에 `Line SEP 배수(line_route_offset_scale)`를 추가해 LINE/corridor 경로 waypoint의 sweep 대비 lateral offset을 GUI와 runtime 설정에서 직접 조정할 수 있도록 연결함. 기존 `Area SEP 배수(area_route_offset_scale)`는 그대로 유지하며, 두 값 모두 기본 1.0이라 값을 바꾸지 않으면 기존 동작을 유지함.
2026-04-02 v1.3.13 - 공간해상도(GSD) 만족 판정 기준을 기존 축별 GSD 비교(gsd_x ≤ req_gsd_x AND gsd_y ≤ req_gsd_y)에서 논문(JANT 2024) 기반 면적 GSD로 교체함. γ = (객체 가로 × 객체 세로) / (최소 픽셀 가로 × 최소 픽셀 세로) [m²/px], 허용 최대 풋프린트 면적 s_req = γ × (이미지 총 픽셀)로 계산하며, fp_w × fp_h ≤ s_req 일 때 만족으로 판정함. 차트 기준선은 √γ (등가 선형 GSD)로 표시하고, 폼에 실제 풋프린트 면적과 요구 면적을 함께 노출함.
2026-04-02 v1.3.12 - UAV 탐색 WP 고도 계산을 전면 개선함. ① 고도 기준을 기존 경로 샘플링 (min+max)/2에서 해당 WP의 coordinateList 3점(p1·중심·p2) DEM 정확 평균값으로 교체함. ② 이전 WP 고도에서 climb_rate×(거리/속도)를 초과하는 상승은 물리적으로 도달 불가 고도로 보고 캡을 적용하는 상승 제약을 추가함. ③ UAV 상승률(m/s) 파라미터를 algo_config_tab "WP/선회" 그룹에 GUI 항목으로 노출하고 uav_params.json에 저장되도록 연결함(기본값 5.0). ④ Area 임무에서 WP 고도를 pack 이전에 개별 WP별로 먼저 산정한 뒤, 그룹 내 최고 고도를 packed WP에 보존하도록 순서를 재조정해 실제 지형 기복에 따라 packed WP 간 고도가 오르내리도록 개선함.
2026-04-02 v1.3.11 - 촬영품질 모니터 탭에 공간해상도(GSD) 모니터링 섹션을 추가함. 0401 footprintCornerList 4점 좌표로 실제 촬영 폭·높이를 계산하고, 이미지 해상도(기본 1920×1080)로 나눠 GSD(m/px)를 산출한 뒤, 탐지 요구조건(객체 크기÷최소 픽셀)과 비교하여 만족/미달 여부 및 최근 300회 누적 만족률을 무인기별 카드와 시계열 차트로 표시함. 이미지 크기·객체 크기·최소 픽셀값은 UI SpinBox로 실시간 변경 가능함.
2026-04-02 v1.3.10 - 시뮬레이션이 협업기저임무 경계에서 0803 execute=1 수신 시 자동으로 다음 임무로 진행하도록 수정하고, 웹 UI의 직접 `/api/sim/next_mission` 호출을 제거해 UAV 전진과 모니터링 상태 전환이 동일한 0803 수신 시점에 동기화되도록 레이스 컨디션을 해소함.
2026-04-02 v1.3.09 - 공격 임무 재계획에서 source MissionPlan/IMP/FlightPath 중복 로딩과 done-input 스캔을 1회 캐시로 정리하고, 단계별 timing 로그를 추가해 로직/ID 체계 변경 없이 병목을 추적하고 처리 시간을 줄이도록 보강함.
2026-04-02 v1.3.09 - 임무계획 GUI의 `협업기저 재계획 테스트` 탭을 제거하고, 다음 협업기저임무 재계획의 T' 진입 기준을 최대 SEP의 단순 0.8 배수가 아니라 해당 비율 이하의 실제 FOV DB 행을 선택해 그 행의 SEP/FOV/속도를 함께 적용하도록 조정함.
2026-03-31 v1.3.08 - 다음 협업기저임무 인가 시 자동 재계획(WithoutPilotDecision)을 수행하도록 적용하고, 협업기저임무 진입 시 leg를 무인기의 실시간 위치 및 속도(벡터)를 고려하여 유동적으로 생성하도록 개선함.
2026-03-31 v1.3.07 - 임무 계획의 FOV 기준을 사람이 아닌 탱크 기준으로 변경하고, 탱크 발견 후 줌인 도중 사람을 발견한 경우에는 사람을 타게팅하도록 로직을 적용함.
2026-03-31 v1.3.06 - 유인기가 비가용(health==2) 상태일 때 경고 메시지를 남기는 로직을 추가하고, 해당 기체를 제외한 재계획이 자동 발동되도록 적용함.
2026-03-31 v1.3.05 - 선회모니터에서 raw heading 대신 좌표 기반 fallback이 우선 적용되어 헤딩이 순간적으로 90도 꺾이며 팅기던 문제를 수정함.
2026-03-31 v1.3.04 - 선행임무에 대한 FOV를 사람 기준에서 탱크 기준으로 변경함.
2026-03-31 v1.3.03 - 촬영품질 모니터 용어를 `촬영품질`로 통일하고, 품질 기준값을 `sep` 단독이 아닌 `sqrt(sep² + (width/2)²)` 대각선 maximum으로 재정의함.
2026-03-31 v1.3.02 - 협업기저임무 완료 처리에서 `0803 execute=1` 중복 반영을 방지하여, 완료 신호가 두 번 날아가며 다음 임무가 두 번 넘어간 것처럼 보이던 문제를 수정함.
2026-03-31 v1.3.01 - Area 임무에서 Sweep 중간점이 정상 반영되지 않던 문제를 수정하여, sweep 정렬·구간 병합·segment 방향 처리가 초기임무계획과 동일하게 동작하도록 보정함.
2026-03-26 v1.2.18 - SIM 적 표적 시각화를 전면 고급화해 전차/장갑차/방사포/곡사포/고정고사포/군인별 3D 형상을 서로 다르게 렌더링하고, 이동 속도 기반 trail·탐지 halo·사격 ring·피격 후 침묵한 잔존 표현까지 함께 표시하도록 개선함.
2026-03-26 v1.2.17 - SIM 적 표적 상태 직렬화에 heading·headingRate·실속도·속도범위·roam 반경·탐지 여부·노출 시간·탄약·무기 종류/사거리/재장전·마지막 발사 경과시간을 추가하고, 정지형 대공/포대는 실제 교전 대상 방향을 보도록 heading을 갱신해 시각 모델과 교전 방향이 어긋나지 않게 보강함.
2026-03-26 v1.2.16 - SIM 내부 nFusion 연결을 다시 `nFusionSettings.json`·`current_scenario.json` 기반으로 정리하고, `sim_main`은 `0.0.0.0`에 바인드한 뒤 충돌 없는 빈 포트를 자동 선택해 다른 PC에서도 안정적으로 접속되도록 복구함.
2026-03-26 v1.2.15 - SIM 모니터링 모드를 `RX 0401 @ 5Hz` 최신 프레임 기준 시각화로 정리하고, 실시간 임무 변경에도 재시각화가 이어지도록 하며 latest-only polling과 mission payload 최소화로 steady-state 부하를 크게 줄임.
2026-03-26 v1.2.14 - 비행 경로 시각화에 waypoint `Fly-by/Fly-over/Loiter` 유형 표시와 현재 추종 WP 강조를 추가하고, 0401/라이브 상태와 패널 표시를 맞춰 실제 추종 중인 임무 진행 상태를 더 직접적으로 읽을 수 있게 개선함.
2026-03-26 v1.2.13 - SIM 고속 재생 시 끊김을 줄이도록 history 처리·footprint/trail 갱신 주기를 최적화하고, DEM blank tile fallback·terrain/hillshade source 분리·geometry 경고 완화까지 묶어 지도 렌더링 안정성을 보강함.
2026-03-26 v1.2.12 - SIM 웹 UI를 dark 톤 기준으로 다시 정리해 좌상단 Visual Mode 카드, 시나리오 입력부, 상단 toolbar, flight-path legend, 우하단 맵 컨트롤의 색감과 계층을 통일하고 monitoring/agent panel 겹침까지 정리함.
2026-03-26 v1.2.11 - SIM 조작 디테일을 다듬어 좌우 패널 화살표 hover 튐을 제거하고, 적 배치 radial menu 간격을 재조정했으며, 3D building 농도도 소폭 올려 배경 지형 위에서 형태가 더 또렷하게 보이도록 보정함.
2026-03-26 v1.2.10 - 모니터링 시각화 구성을 재정비해 `실시간 위험도 예측`을 단독 탭으로 분리하고, 기존 시각화 탭은 임무 상태 카드·표 정렬을 다시 맞춰 더 넓고 균일하게 보이도록 재배치함.
2026-03-26 v1.2.9 - 스케줄 모니터 탭의 재계획 버튼을 모두 제거해 표시 전용으로 단순화하고, 관련 제어는 `임무 재계획 관리` 탭에서만 통합 관리하도록 정리함.
2026-03-26 v1.2.8 - `임무 재계획 관리` 탭을 신설해 경로추종·촬영품질·강제대기·입력갱신·선행임무·DL 위험·촬영계획·다음협업·RTB·표적탐지·연료 등의 ON/OFF와 세부 파라미터를 한곳에서 조정하도록 연결하고, 탭 순서도 `모니터링 CSC` 바로 뒤로 재배치함.
2026-03-26 v1.2.7 - 재계획 설정의 현재값과 권장값을 JSON으로 분리 저장하고, 재시작 후에도 마지막 설정을 복원하며 권장값 복원/저장까지 GUI에서 직접 수행할 수 있도록 구성함.
2026-03-26 v1.2.6 - 재계획 관리 탭 UI를 전체 스크롤 카드형 레이아웃으로 재구성하고, 마우스 휠 오입력 방지·스핀 화살표 아이콘·정렬·색감·용어(`선행임무`)를 다듬어 다른 모니터링 화면과 통일감을 맞춤.
2026-03-26 v1.2.5 - 경로 미추종 재계획 기준을 runtime 설정으로 외부화하고, 선회율·경고각·누적시간·대체 WP 생성 지연 등 숨은 파라미터까지 GUI에 노출해 더 타이트하게 조정할 수 있도록 보강함.
2026-03-26 v1.2.4 - `0401 선회 모니터`를 `경로추종 모니터링`으로 개편하고, 밝은 톤 UI와 HUD 정렬을 정비했으며 현재 WP의 `Fly By/Fly Over/Loiter` 타입을 카드와 지도에서 직접 확인할 수 있도록 개선함.
2026-03-26 v1.2.3 - `0401` heading 판정에서 raw heading을 우선 사용하고 좌표 기반 fallback을 보수화했으며, 중복 `0401` 처리도 정리해 순간적인 90도 꺾임 표시와 선회 재계획 누락 가능성을 줄임.
2026-03-26 v1.2.2 - 촬영품질 모니터 용어를 `촬영품질`로 통일하고, 품질 기준값을 `sep` 단독이 아닌 `sqrt(sep^2 + (width/2)^2)`로 재정의했으며 이때 width는 `0401 sensorInfo.fov`에 해당하는 DB 구간의 최대 width를 사용하도록 보정함.
2026-03-26 v1.2.1 - 협업기저임무 완료 처리에서 `0803 execute=1` 중복 반영을 막고 transient `100% -> 하락 -> 100%` 구간이 있더라도 다음 임무가 두 번 넘어간 것처럼 보이지 않도록 mission progress 연계 처리를 보강함.
2026-03-21 v1.1.33 - 협업기저·선행임무 재계획의 LINE/corridor 생성에서 sweep 정렬·구간 병합·segment 방향 처리를 보정해, 초기임무계획처럼 여러 sweep가 한 waypoint에 정상 묶이도록 수정함.
2026-03-21 v1.1.33 - non-area 재계획 경로의 lineSearch 순서 뒤집힘과 잘못된 POINT 변환을 정리해, 끝까지 갔다가 다시 돌아오는 경로와 마지막 sweep 유실 문제를 보정함.
2026-03-21 v1.1.33 - 모니터링의 `다음 협업기저임무 수행` 명령이 즉시 다음 input mission을 active 대상으로 전환하도록 수정하고, 다음 임무가 없을 때는 `0001`로 `모니터링 모듈: 다음 협업기저임무가 없습니다.`를 통지하도록 보완함.
2026-03-21 v1.1.33 - 다음 협업기저임무 재계획 payload가 없을 때도 `execute=1` 일반 처리로 정상 fallback 되도록 보정하고, 해당 기능의 기본값은 다시 OFF로 정리함.
2026-03-21 v1.1.33 - `정찰/시간 균형` 재계획이 현재 초기임무계획 세팅을 그대로 따르도록 정리하고, 초기임무계획 알고리즘 설정 GUI는 실제 연결된 항목만 남긴 구조로 재정비함.
2026-03-21 v1.1.33 - Line/Area FOV Auto·Custom, Area FOV 배수, Area SEP 배수, Area 나누는 폭, UAV/LAH WP 간격, Dubins 선회반경을 GUI에서 조정하고 runtime에 직접 반영하도록 연결함.
2026-03-21 v1.1.33 - 재계획·이어붙이기 경로의 waypointID 재사용을 제거해 유인기·무인기를 포함한 전체 waypointID가 전역 유일하게 증가하도록 보정함.
2026-03-21 v1.1.33 - 유인헬기 공격 재계획에서 비선정 LAH의 hold/resume 진입점 처리와 현재 waypoint 제거를 정리하고, 공격 분석 반경은 2km로 축소했으며 `onMission=2` UAV는 경로 미추종 재계획에서 제외함.
2026-03-21 v1.1.33 - `modules/monitoring`의 PyInstaller build 산출물, `__pycache__`, 백업 파일 등 기능과 무관한 생성물을 정리해 용량을 축소함.
2026-03-20 v1.1.32 - SIM 웹에 `filmingProperty.lineSearch.coordinateList` 기반 실제 sweep 선/점 토글을 추가하고 얇은 점선 스타일로 조정했으며, 파괴된 표적은 `targetInfo.isDestroyed` 상태를 기준으로 자동추적/재추적하지 않도록 보정함.
2026-03-20 v1.1.32 - 좌표 임무 기본 FOV를 31.2도로 조정하고, AREA 임무도 실제 폭과 `enhanced_area_review_max_segment_m=500m` 기준으로 DB FOV/SEP를 선택하도록 확장했으며, AREA 수색 `searchSpeed` 가중치는 1.2로 상향함.
2026-03-20 v1.1.32 - AREA 경로계획에서 첫 chunk waypoint를 첫 실제 sweep line 기준 SEP 이격으로 재배치하고, 시작 anchor의 진행방향 lead-in 및 고도를 첫 chunk와 맞추며, 연속 AREA 임무는 offset 방향이 번갈아 적용되도록 정리함.
2026-03-20 v1.1.32 - AREA 간 Dubins flyover link는 옵션으로 켜고 끌 수 있게 정비하고, 가까운 transition link 및 중복 entry anchor는 자동으로 생략하며, 다음 AREA 시작 side 판단은 이전 임무의 마지막 waypoint 좌표 기준으로 보정함.
2026-03-20 v1.1.32 - 공격 재계획 시 유인헬기(LAH)는 완료된 done 개별임무/path를 별도로 보존하지 않고 `attack 또는 hold + resume + follow-up`만 남기도록 재구성했으며, Dubins turn link GUI의 import 경로도 직접 실행 가능하게 수정함.
2026-03-19 v1.1.31 - 다음 협업기저임무 재계획이 `WaypointID pool exhausted`로 실패하던 문제를 수정하고, 0303/0304 local waypoint allocator가 현재 906xxx대 waypoint ID 체계에서도 정상 동작하도록 65,535 상한 체크를 제거함.
2026-03-19 v1.1.30 - 모니터링 mission progress tracker가 같은 UAV의 mission sequence에서 더 뒤 individual mission으로 점프할 때 inputMission 경계를 넘더라도 중간에 건너뛴 individual mission과 input mission을 완료 처리하도록 보정함.
2026-03-19 v1.1.24 - 모니터링 mission tracker snapshot에 aircraft별 실제 current individual mission과 active input mission을 노출하고, 시각화 탭·스케줄 탭·0501 payload가 DB의 next pending mission 대신 0401/currentWP 기반 현재 수행 임무를 따라가도록 보정함.
2026-03-19 v1.1.23 - 경로 미추종 재계획 0902 사유 문구의 고정 placeholder `000 무인기`를 제거하고, aircraftID 4/5/6을 실제 `무인기 1/2/3번`으로 매핑해 replanRequest/replanReason/replanDetail.selectedReplanReason에 반영하도록 수정함.
2026-03-19 v1.1.22 - UAV 0303 search waypoint 간격 기준을 800m로 조정하고, 각 search waypoint 고도를 해당 구간 지형의 최고/최저 고도 중간값에 UAV별 고도층 610/620/630m를 더한 값으로 재산정하도록 보정함.
2026-03-19 v1.1.21 - LAH-UAV ETA 기반 속도조정 후 최종 speed profile은 LAH 1번을 기준으로 다른 LAH에 동기화하고, LAH 1번이 없으면 LAH 2번을 기준으로 따르도록 보정해 편대가 같은 속도 프로파일로 움직이게 수정함.
2026-03-19 v1.1.20 - SIM spawn 좌표를 takeOverInfo보다 FlightPath 첫 waypoint 기준으로 우선하도록 바꿔, 유인기를 포함한 기체가 시작 시 첫 waypoint 위치와 고도로 바로 배치되게 수정함.
2026-03-19 v1.1.19 - LAH-UAV ETA follow speed에서 UAV `eta`는 초, LAH `eta`는 밀리초로 저장되던 단위 차이와 aircraft 단위 단일 path 참조 문제를 수정해, 같은 짝의 UAV path 중 시작/끝 좌표가 가장 가까운 경로를 기준으로 LAH waypoint speed가 실제로 재계산되도록 보정함.
2026-03-19 v1.1.18 - mission_planning_gui의 실제 FlightPath 생성 경로 두 곳에도 LAH-UAV ETA follow speed 후처리를 연결해, 실행 계획에서 LAH waypoint speed가 기본값 15.0으로만 남지 않고 paired UAV ETA 기준으로 다시 계산되도록 수정함.
2026-03-19 v1.1.17 - LAH 0304 생성 후 paired UAV(1↔4, 2↔5, 3↔6)의 cumulative ETA를 기준으로 UAV 예상 위치를 보간해, LAH 경로 좌표는 유지한 채 waypoint speed/eta만 후처리하여 UAV보다 먼저 가지 않으면서도 과도하게 뒤처지지 않도록 추종 속도 계획을 추가함.
2026-03-19 v1.1.16 - LAH 경로에서 fallback 직선 분할뿐 아니라 정상 route planner 결과도 500m 간격으로 재샘플링해 긴 leg가 남지 않도록 보정함.
2026-03-19 v1.1.15 - LINE/corridor 수색에서 500m 간격 bundle waypoint를 만들 때 SEP offset anchor(`offset_wps`/`orange_pts`) 좌표를 실제 waypoint로 사용하도록 수정해 첫 점뿐 아니라 이후 bundle waypoint도 lateral offset을 유지하게 정리함.
2026-03-19 v1.1.14 - 촬영품질 확인 탭 기본값을 모니터링 ON, 재계획 판단 OFF로 분리해 그래프와 상태 확인은 유지하면서 자동 품질 0902는 기본 비활성으로 변경함.
2026-03-19 v1.1.13 - LINE/AREA 수색 경로 waypoint를 약 500m 간격으로 재구성해 한 waypoint가 과도한 sweep 묶음을 들고 가지 않도록 조정함.
2026-03-19 v1.1.12 - 촬영품질 모니터 자동 재계획 기본값을 OFF로 변경하고 기본 분기를 보수적으로 조정해 품질 0902가 기본 발동하지 않도록 수정함.
2026-03-19 v1.1.11 - 품질 재계획 적용 후 FlightPath 앞부분의 `isDone=true` waypoint를 SIM controller가 다시 따라가지 않도록 첫 미완료 waypoint부터 재개하게 수정함.
2026-03-19 v1.1.10 - 촬영품질 기반 자동 재계획에 MissionPlan 적용 직후 10초 grace를 추가하고 해당 구간은 샘플을 누적하지 않도록 조정함.
2026-03-19 v1.1.9 - 0902 CLR 전송에서 `missionPlanIDList`와 `replanDetail` 같은 custom field 누락을 sidecar transport store로 보완해 품질개선 direct 파이프라인이 plan ID/detail 없이 깨지지 않도록 복구함.
2026-03-19 v1.1.8 - 품질개선 재계획을 선행임무/다음임무와 같은 direct 처리로 고정하고 0901/0701 옵션 UI가 생성되지 않도록 수신·전달 단계의 차단 조건을 보강함.
2026-03-19 v1.1.7 - 품질개선 0902 수신 시 `missionPlanIDList`가 비어 있어도 `replanDetail.missionPlanID`로 plan ID를 복구하고, `force_direct_update`와 `suppress_0702_fallback`을 강제해 옵션 경로로 빠지지 않도록 보완함.
2026-03-19 v1.1.6 - 0401 기반 촬영품질 평균으로 UAV별 `searchSpeed`를 ±10% 보정하는 품질개선 재계획을 추가하고, `flightMode=8`에서는 비활성화하며 옵션 없는 `0902 -> direct 0301/0305/0903` 경로로 처리하도록 구성함.
2026-03-19 v1.1.6 - 품질개선 재계획에 현재 UAV 위치 anchor, 기존 sweep 진행분 보존, 남은 sweep trim과 speed scale 반영을 추가하고 사유를 `UAV N 촬영 품질 개선`으로 기록하도록 정리함.
2026-03-19 v1.1.5 - 모니터링에 촬영품질 확인 탭을 추가하고, 0401 기반 UAV SEP 추이·기준 SEP·유인기-무인기 거리 그래프를 실시간 시각화하도록 구성함.
2026-03-19 v1.1.4 - 0401의 payload 고장과 LAH-UAV datalink 두절은 0902를 보내지 않고 0001 고장/통신두절 통지만 보내도록 정리했으며, 연료 경고는 0504만 유지하도록 정비함.
2026-03-19 v1.1.4 - UAV `health==2`는 즉시 비가용으로 판단해 해당 기체를 제외한 0902를 발동하고, 초기 임무계획은 `InputMissionPlan/100`의 `availableAircraftList`를 기준으로 가용 기체를 구성하도록 수정함.
2026-03-19 v1.1.4 - 0001 통지 문구를 유인기/무인기 번호 기반으로 정리하고, 통신두절은 LAH-UAV pair별로 `유인기 N - 무인기 N 통신두절` 형태로 출력하도록 변경함.
2026-03-19 v1.1.4 - 모니터링 GUI의 기체 상태/0401 신호 상태 갱신 충돌을 분리하고, 신호 판정은 GUI 수신 시각 기준으로 바꿔 상태 깜빡임을 줄이도록 보정함.
2026-03-19 v1.1.4 - 새 0201에서 `isDone=false`인 임무의 `inputMissionType=0`이 line/area 도형이면 자동 보정하고, 0001 임무 type 이상 경고만 띄운 채 임무계획은 계속 진행하도록 처리함.
2026-03-19 v1.1.4 - 공격배제로 처리된 same `targetID`도 선행임무(표적추적/좌표지향)로 다시 발견하면 rediscovery 상태를 기록해 `isUsed/isIgnored`를 다시 0으로 내리고, 실제 0902가 생성되는 시점에만 다시 consume하도록 수정함.
2026-03-17 v1.1.3 - Dubins Mode preset/UAV plan mode를 추가하고 division test 및 LINE/AREA 경로 생성, 시작점/진입점 처리, sweep 표시를 정비함.
2026-03-17 v1.1.3 - UAV별 take-over 기준 분할/스케줄링, RTB 재계획 지연·우선순위·팝업, LAH datalink 기반 통신두절 판정과 resume sweep/search speed 보정을 반영함.
2026-03-17 v1.1.3 - SIM에 SEP 표시와 초과 경고를 추가하고 공격배제 시 추적을 종료한 뒤 새 임무 수행으로 복귀하도록 수정했으며 공격 임무 계산 성능을 개선함.
2026-03-16 v1.1.2 - 0201 InputMission에 regionType 필드를 반영하고 common generator/push/receive 직렬화를 갱신함.
2026-03-16 v1.1.2 - 0104 공격통제모듈 상태정보의 common generator/push/receive를 추가하고 모든 모듈 수신 및 정보관리 송수신을 허용함.
2026-03-16 v1.1.1 - change_log.md 파일을 생성하고 변경 이력을 한 줄 요약 형식으로 누적하기 시작함.
2026-03-16 v1.1.1 - SIM 모듈 기본 서버 호스트를 192.168.100.251에서 127.0.0.1로 변경함.
2026-03-19 v1.1.25 - AREA 임무 entry waypoint가 측면 reference 선택이 아니라, 최종 area 경로의 첫 번째 leg 방향을 기준으로 뒤로 entry_offset만큼 직선 offset 되도록 보정함.
2026-03-19 v1.1.26 - SIM mission preserve-state 재로딩 시 path가 바뀌지 않은 비재계획 기체의 controller curr_idx/block/loiter 및 input mission index를 유지해, 경로 미추종 재계획 때 다른 UAV가 새 plan 첫 상태로 다시 해석되며 다음 임무로 넘어간 것처럼 보이던 문제를 보정함.
2026-03-19 v1.1.28 - AREA 임무의 기본 0303 경로와 다음 협업기저임무 전용 재계획 경로에서 보조 entry waypoint를 생성하지 않도록 수정해, AREA 경로가 첫 search waypoint부터 바로 시작되게 보정함.
2026-03-19 v1.1.29 - 다음 협업기저임무 전용 재계획에서 AREA local area-review가 `missionReferencePackageID=0`일 때 `0.json`을 읽지 못해 `missing_takeover`로 최대 폭 분할이 스킵되던 문제를 수정하고, takeover 정보가 비어 있으면 `entryAircraftList` 좌표를 `takeOverInfoList` fallback으로 주입해 AREA 세분화가 계속 적용되게 보정함.
2026-04-02 v1.3.19 - 표적 탐지 재계획 후 sweep-progress trim이 남은 waypoint를 전부 제거하게 되는 경우에도 Attack UAV tracking/resume split이 resume branch를 보존하도록 수정해, 탐지한 UAV가 다음 임무로 바로 건너뛰는 문제를 방지함.
2026-05-29 v1.3.32 - Replan replay workflow added and general 3-option store pathID cross-variant bulk reservation restored to reduce replay/sim store-tail spikes.
2026-05-29 v1.3.33 - sim_main now persists 0902/0305 popup timing evidence under DSS_Internal/sim_payload_observations so Phase 2 popup E2E checks can run from scenario logs without manually copying integration payloads.
2026-05-29 v1.3.34 - Popup timing reports now prefer the 0305 observation record with displayedAtMs when duplicate receive/display records share the same observedAtMs.
2026-05-29 v1.3.35 - Replan performance verifier now auto-discovers sim_main popup proof payloads from DSS_Internal/sim_payload_observations when --require-popup-proof is used.
2026-05-29 v1.3.36 - Replan performance verifier now exposes and gates parallel_thread_budget ratio so worker-count tuning must pass an explicit peak_configured_threads/cpu_count limit.
2026-05-29 v1.3.37 - Replan performance verifier can now require popupDisplayedAt evidence so headless-only payload timing cannot close the real popup E2E gate.
2026-05-29 v1.3.38 - 0303 groundRequired waiter variants no longer duplicate dense local recompute by default; same-payload replay keeps the waiter batch local fast path off after it reduced status2 and slowest 0303 time.
2026-05-29 v1.3.39 - FOV DB row reads now reuse the process-level path/mtime/size cache across planning paths; latest real sim and replay evidence no longer shows repeated FOV DB load lines in the general 3-option path.
2026-05-29 v1.3.40 - 0303 dependency-parallel formation contract now explicitly guards leader-before-follower output order and follower leaderAircraftID integrity before any further DAG refactor.
